{{alias}}( iseed, strideISEED, N, x, strideX )
    Returns a vector of n random real numbers from a uniform (0,1)
    distribution.

    Parameters
    ----------
    iseed: Float64Array
        `iseed`.

    strideISEED: integer
        stride length for `ISEED`.

    N: integer
        number of columns.

    x: Float64Array
        `x`.

    strideX: integer
        stride length for `X`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( iseed, strideISEED, offsetISEED, N, x, strideX, offsetX )
    Returns a vector of n random real numbers from a uniform (0,1)
    distribution using alternative indexing semantics.

    Parameters
    ----------
    iseed: Float64Array
        `iseed`.

    strideISEED: integer
        stride length for `ISEED`.

    offsetISEED: integer
        starting index for `ISEED`.

    N: integer
        number of columns.

    x: Float64Array
        `x`.

    strideX: integer
        stride length for `X`.

    offsetX: integer
        starting index for `X`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
