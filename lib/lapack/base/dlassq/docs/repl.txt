{{alias}}( N, x, stride, scale, sumsq )
    Returns updated scale and sum-of-squares in scaled form.

    Parameters
    ----------
    N: integer
        number of columns.

    x: number
        `x`.

    stride: integer
        `stride`.

    scale: number
        `scale`.

    sumsq: number
        `sumsq`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, x, stride, offset, scale, sumsq )
    Returns updated scale and sum-of-squares in scaled form using
    alternative indexing semantics.

    Parameters
    ----------
    N: integer
        number of columns.

    x: number
        `x`.

    stride: integer
        `stride`.

    offset: integer
        `offset`.

    scale: number
        `scale`.

    sumsq: number
        `sumsq`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
