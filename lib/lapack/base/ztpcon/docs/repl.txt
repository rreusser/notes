{{alias}}( norm, uplo, diag, N, AP, RCOND, WORK, RWORK )
    Estimates the reciprocal condition number of a complex triangular matrix
    in packed storage.

    Parameters
    ----------
    norm: string
        `norm`.

    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    diag: string
        specifies whether the matrix is unit triangular.

    N: integer
        number of columns.

    AP: Float64Array
        `AP`.

    RCOND: number
        `RCOND`.

    WORK: Float64Array
        `WORK`.

    RWORK: Float64Array
        `RWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( norm, uplo, diag, N, AP, strideAP, offsetAP, RCOND, WORK, strideWORK, offsetWORK, RWORK, strideRWORK, offsetRWORK )
    Estimates the reciprocal condition number of a complex triangular matrix
    in packed storage using alternative indexing semantics.

    Parameters
    ----------
    norm: string
        `norm`.

    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    diag: string
        specifies whether the matrix is unit triangular.

    N: integer
        number of columns.

    AP: Float64Array
        `AP`.

    strideAP: integer
        stride length for `AP`.

    offsetAP: integer
        starting index for `AP`.

    RCOND: number
        `RCOND`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    RWORK: Float64Array
        `RWORK`.

    strideRWORK: integer
        stride length for `RWORK`.

    offsetRWORK: integer
        starting index for `RWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
