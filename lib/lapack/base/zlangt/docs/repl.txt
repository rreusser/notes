{{alias}}( norm, N, DL, strideDL, d, strideD, DU, strideDU )
    Returns the norm of a complex general tridiagonal matrix A.

    Parameters
    ----------
    norm: string
        `norm`.

    N: integer
        number of columns.

    DL: Float64Array
        `DL`.

    strideDL: integer
        stride length for `DL`.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    DU: Float64Array
        `DU`.

    strideDU: integer
        stride length for `DU`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( norm, N, DL, strideDL, offsetDL, d, strideD, offsetD, DU, strideDU, offsetDU )
    Returns the norm of a complex general tridiagonal matrix A using
    alternative indexing semantics.

    Parameters
    ----------
    norm: string
        `norm`.

    N: integer
        number of columns.

    DL: Float64Array
        `DL`.

    strideDL: integer
        stride length for `DL`.

    offsetDL: integer
        starting index for `DL`.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    offsetD: integer
        starting index for `D`.

    DU: Float64Array
        `DU`.

    strideDU: integer
        stride length for `DU`.

    offsetDU: integer
        starting index for `DU`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
