{{alias}}( order, jobu, jobvt, M, N, A, LDA, s, strideS, U, LDU, VT, LDVT, WORK, strideWORK, lwork, RWORK, strideRWORK )
    Computes the singular value decomposition (SVD) of a complex M-by-N
    matrix A.

    Parameters
    ----------
    order: string
        Row-major (C-style) or column-major (Fortran-style) order.

    jobu: string
        `jobu`.

    jobvt: string
        `jobvt`.

    M: integer
        number of rows.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    s: Float64Array
        `s`.

    strideS: integer
        stride length for `S`.

    U: Float64Array
        `U`.

    LDU: integer
        leading dimension of `U`.

    VT: Float64Array
        `VT`.

    LDVT: integer
        leading dimension of `VT`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    lwork: integer
        `lwork`.

    RWORK: Float64Array
        `RWORK`.

    strideRWORK: integer
        stride length for `RWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( jobu, jobvt, M, N, A, strideA1, strideA2, offsetA, s, strideS, offsetS, U, strideU1, strideU2, offsetU, VT, strideVT1, strideVT2, offsetVT, WORK, strideWORK, offsetWORK, lwork, RWORK, strideRWORK, offsetRWORK )
    Computes the singular value decomposition (SVD) of a complex M-by-N
    matrix A, using alternative indexing semantics.

    Parameters
    ----------
    jobu: string
        `jobu`.

    jobvt: string
        `jobvt`.

    M: integer
        number of rows.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    s: Float64Array
        `s`.

    strideS: integer
        stride length for `S`.

    offsetS: integer
        starting index for `S`.

    U: Float64Array
        `U`.

    strideU1: integer
        stride of dimension 1 of `U`.

    strideU2: integer
        stride of dimension 2 of `U`.

    offsetU: integer
        starting index for `U`.

    VT: Float64Array
        `VT`.

    strideVT1: integer
        stride of dimension 1 of `VT`.

    strideVT2: integer
        stride of dimension 2 of `VT`.

    offsetVT: integer
        starting index for `VT`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    lwork: integer
        `lwork`.

    RWORK: Float64Array
        `RWORK`.

    strideRWORK: integer
        stride length for `RWORK`.

    offsetRWORK: integer
        starting index for `RWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
