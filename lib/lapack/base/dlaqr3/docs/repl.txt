{{alias}}( wantt, wantz, N, ktop, kbot, nw, H, LDH, iloz, ihiz, Z, LDZ, SR, strideSR, SI, strideSI, V, LDV, nh, T, LDT, nv, WV, LDWV, WORK, strideWORK, lwork )
    Performs aggressive early deflation on an upper Hessenberg matrix.

    Parameters
    ----------
    wantt: boolean
        `wantt`.

    wantz: boolean
        `wantz`.

    N: integer
        number of columns.

    ktop: number
        `ktop`.

    kbot: number
        `kbot`.

    nw: integer
        `nw`.

    H: Float64Array
        `H`.

    LDH: integer
        leading dimension of `H`.

    iloz: number
        `iloz`.

    ihiz: number
        `ihiz`.

    Z: Float64Array
        `Z`.

    LDZ: integer
        leading dimension of `Z`.

    SR: Float64Array
        `SR`.

    strideSR: integer
        stride length for `SR`.

    SI: Float64Array
        `SI`.

    strideSI: integer
        stride length for `SI`.

    V: Float64Array
        `V`.

    LDV: integer
        leading dimension of `V`.

    nh: integer
        `nh`.

    T: Float64Array
        `T`.

    LDT: integer
        leading dimension of `T`.

    nv: integer
        `nv`.

    WV: Float64Array
        `WV`.

    LDWV: integer
        leading dimension of `WV`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    lwork: integer
        `lwork`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( wantt, wantz, N, ktop, kbot, nw, H, strideH1, strideH2, offsetH, iloz, ihiz, Z, strideZ1, strideZ2, offsetZ, ns, nd, SR, strideSR, offsetSR, SI, strideSI, offsetSI, V, strideV1, strideV2, offsetV, nh, T, strideT1, strideT2, offsetT, nv, WV, strideWV1, strideWV2, offsetWV, WORK, strideWORK, offsetWORK, lwork )
    Performs aggressive early deflation on an upper Hessenberg matrix using
    alternative indexing semantics.

    Parameters
    ----------
    wantt: boolean
        `wantt`.

    wantz: boolean
        `wantz`.

    N: integer
        number of columns.

    ktop: number
        `ktop`.

    kbot: number
        `kbot`.

    nw: integer
        `nw`.

    H: Float64Array
        `H`.

    strideH1: integer
        stride of dimension 1 of `H`.

    strideH2: integer
        stride of dimension 2 of `H`.

    offsetH: integer
        starting index for `H`.

    iloz: number
        `iloz`.

    ihiz: number
        `ihiz`.

    Z: Float64Array
        `Z`.

    strideZ1: integer
        stride of dimension 1 of `Z`.

    strideZ2: integer
        stride of dimension 2 of `Z`.

    offsetZ: integer
        starting index for `Z`.

    ns: number
        `ns`.

    nd: number
        `nd`.

    SR: Float64Array
        `SR`.

    strideSR: integer
        stride length for `SR`.

    offsetSR: integer
        starting index for `SR`.

    SI: Float64Array
        `SI`.

    strideSI: integer
        stride length for `SI`.

    offsetSI: integer
        starting index for `SI`.

    V: Float64Array
        `V`.

    strideV1: integer
        stride of dimension 1 of `V`.

    strideV2: integer
        stride of dimension 2 of `V`.

    offsetV: integer
        starting index for `V`.

    nh: integer
        `nh`.

    T: Float64Array
        `T`.

    strideT1: integer
        stride of dimension 1 of `T`.

    strideT2: integer
        stride of dimension 2 of `T`.

    offsetT: integer
        starting index for `T`.

    nv: integer
        `nv`.

    WV: Float64Array
        `WV`.

    strideWV1: integer
        stride of dimension 1 of `WV`.

    strideWV2: integer
        stride of dimension 2 of `WV`.

    offsetWV: integer
        starting index for `WV`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    lwork: integer
        `lwork`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
