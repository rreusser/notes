{{alias}}( jobt, N, vl, vu, D, E, pivmin )
    Counts the number of eigenvalues of a symmetric tridiagonal matrix in an
    interval.

    Parameters
    ----------
    jobt: number
        `jobt`.

    N: integer
        number of columns.

    vl: number
        `vl`.

    vu: number
        `vu`.

    D: Float64Array
        `D`.

    E: Float64Array
        `E`.

    pivmin: number
        `pivmin`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( jobt, N, vl, vu, D, strideD, offsetD, E, strideE, offsetE, pivmin )
    Counts the number of eigenvalues of a symmetric tridiagonal matrix in an
    interval using alternative indexing semantics.

    Parameters
    ----------
    jobt: number
        `jobt`.

    N: integer
        number of columns.

    vl: number
        `vl`.

    vu: number
        `vu`.

    D: Float64Array
        `D`.

    strideD: integer
        stride length for `D`.

    offsetD: integer
        starting index for `D`.

    E: Float64Array
        `E`.

    strideE: integer
        stride length for `E`.

    offsetE: integer
        starting index for `E`.

    pivmin: number
        `pivmin`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
