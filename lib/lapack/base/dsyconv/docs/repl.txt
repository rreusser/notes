{{alias}}( uplo, way, N, A, LDA, IPIV, strideIPIV, E, strideE )
    Converts a symmetric matrix given by `dsytrf` (Bunch-Kaufman
    factorization).

    Parameters
    ----------
    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    way: string
        `way`.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    IPIV: Int32Array
        `IPIV`.

    strideIPIV: integer
        stride length for `IPIV`.

    E: Float64Array
        `E`.

    strideE: integer
        stride length for `E`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( uplo, way, N, A, strideA1, strideA2, offsetA, IPIV, strideIPIV, offsetIPIV, E, strideE, offsetE )
    Converts a symmetric matrix given by `dsytrf` (Bunch-Kaufman
    factorization) using alternative indexing semantics.

    Parameters
    ----------
    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    way: string
        `way`.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    IPIV: Int32Array
        `IPIV`.

    strideIPIV: integer
        stride length for `IPIV`.

    offsetIPIV: integer
        starting index for `IPIV`.

    E: Float64Array
        `E`.

    strideE: integer
        stride length for `E`.

    offsetE: integer
        starting index for `E`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
