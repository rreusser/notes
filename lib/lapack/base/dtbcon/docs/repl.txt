{{alias}}( norm, uplo, diag, N, kd, AB, LDAB, rcond, WORK, IWORK )
    Estimates the reciprocal condition number of a real triangular band
    matrix.

    Parameters
    ----------
    norm: string
        `norm`.

    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    diag: string
        specifies whether the matrix is unit triangular.

    N: integer
        number of columns.

    kd: number
        `kd`.

    AB: Float64Array
        `AB`.

    LDAB: integer
        leading dimension of `AB`.

    rcond: number
        `rcond`.

    WORK: Float64Array
        `WORK`.

    IWORK: Int32Array
        `IWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( norm, uplo, diag, N, kd, AB, strideAB1, strideAB2, offsetAB, rcond, WORK, strideWORK, offsetWORK, IWORK, strideIWORK, offsetIWORK )
    Estimates the reciprocal condition number of a real triangular band
    matrix using alternative indexing semantics.

    Parameters
    ----------
    norm: string
        `norm`.

    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    diag: string
        specifies whether the matrix is unit triangular.

    N: integer
        number of columns.

    kd: number
        `kd`.

    AB: Float64Array
        `AB`.

    strideAB1: integer
        stride of dimension 1 of `AB`.

    strideAB2: integer
        stride of dimension 2 of `AB`.

    offsetAB: integer
        starting index for `AB`.

    rcond: number
        `rcond`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    offsetIWORK: integer
        starting index for `IWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
