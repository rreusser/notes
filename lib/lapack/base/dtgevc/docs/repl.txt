
{{alias}}( order, side, howmny, SELECT, strideSELECT, N, S, LDS, P, LDP, VL, LDVL, VR, LDVR, mm, M, WORK, strideWORK )
    Computes some or all of the right and/or left eigenvectors of a pair of real matrices.

    Parameters
    ----------
        order: string
            Row-major (C-style) or column-major (Fortran-style) order.

        side: string
            specifies the operation type.

        howmny: string
            specifies the operation type.

        SELECT: Float64Array
            input array.

        strideSELECT: integer
            stride length for `SELECT`.

        N: integer
            number of columns.

        S: Float64Array
            input matrix.

        LDS: integer
            leading dimension of `S`.

        P: Float64Array
            input matrix.

        LDP: integer
            leading dimension of `P`.

        VL: Float64Array
            input matrix.

        LDVL: integer
            leading dimension of `VL`.

        VR: Float64Array
            input matrix.

        LDVR: integer
            leading dimension of `VR`.

        mm: integer
            mm.

        M: integer
            number of rows.

        WORK: Float64Array
            output array.

        strideWORK: integer
            stride length for `WORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( side, howmny, SELECT, strideSELECT, offsetSELECT, N, S, strideS1, strideS2, offsetS, P, strideP1, strideP2, offsetP, VL, strideVL1, strideVL2, offsetVL, VR, strideVR1, strideVR2, offsetVR, mm, M, WORK, strideWORK, offsetWORK )
    Computes some or all of the right and/or left eigenvectors of a pair of real matrices., using alternative indexing semantics.

    Parameters
    ----------
        side: string
            specifies the operation type.

        howmny: string
            specifies the operation type.

        SELECT: Float64Array
            input array.

        strideSELECT: integer
            stride length for `SELECT`.

        offsetSELECT: integer
            starting index for `SELECT`.

        N: integer
            number of columns.

        S: Float64Array
            input matrix.

        strideS1: integer
            stride of dimension 1 of `S`.

        strideS2: integer
            stride of dimension 2 of `S`.

        offsetS: integer
            starting index for `S`.

        P: Float64Array
            input matrix.

        strideP1: integer
            stride of dimension 1 of `P`.

        strideP2: integer
            stride of dimension 2 of `P`.

        offsetP: integer
            starting index for `P`.

        VL: Float64Array
            input matrix.

        strideVL1: integer
            stride of dimension 1 of `VL`.

        strideVL2: integer
            stride of dimension 2 of `VL`.

        offsetVL: integer
            starting index for `VL`.

        VR: Float64Array
            input matrix.

        strideVR1: integer
            stride of dimension 1 of `VR`.

        strideVR2: integer
            stride of dimension 2 of `VR`.

        offsetVR: integer
            starting index for `VR`.

        mm: integer
            mm.

        M: integer
            number of rows.

        WORK: Float64Array
            output array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: integer
            starting index for `WORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
