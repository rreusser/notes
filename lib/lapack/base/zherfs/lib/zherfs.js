

'use strict';

// MODULES //

var base = require( './base.js' );


// MAIN //

/**
* @license Apache-2.0
*
* @param {number} re - re
* @param {number} im - im
* @returns {integer} info status code
*/
function zherfs( re, im ) {
	return base( re, im );
}


// EXPORTS //

module.exports = zherfs;
