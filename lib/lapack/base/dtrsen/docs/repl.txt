{{alias}}( job, compq, SELECT, strideSELECT, N, T, LDT, Q, LDQ, WR, strideWR, WI, strideWI, M, s, sep, WORK, strideWORK, lwork, IWORK, strideIWORK, liwork )
    Reorders the real Schur factorization of a real matrix A = Q_T_Q**T.

    Parameters
    ----------
    job: string
        `job`.

    compq: string
        `compq`.

    SELECT: Int32Array
        `SELECT`.

    strideSELECT: integer
        stride length for `SELECT`.

    N: integer
        number of columns.

    T: Float64Array
        `T`.

    LDT: integer
        leading dimension of `T`.

    Q: Float64Array
        `Q`.

    LDQ: integer
        leading dimension of `Q`.

    WR: Float64Array
        `WR`.

    strideWR: integer
        stride length for `WR`.

    WI: Float64Array
        `WI`.

    strideWI: integer
        stride length for `WI`.

    M: integer
        number of rows.

    s: number
        `s`.

    sep: number
        `sep`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    lwork: integer
        `lwork`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    liwork: number
        `liwork`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( job, compq, SELECT, strideSELECT, offsetSELECT, N, T, strideT1, strideT2, offsetT, Q, strideQ1, strideQ2, offsetQ, WR, strideWR, offsetWR, WI, strideWI, offsetWI, M, s, sep, WORK, strideWORK, offsetWORK, lwork, IWORK, strideIWORK, offsetIWORK, liwork )
    Reorders the real Schur factorization of a real matrix A = Q_T_Q**T,
    using alternative indexing semantics.

    Parameters
    ----------
    job: string
        `job`.

    compq: string
        `compq`.

    SELECT: Int32Array
        `SELECT`.

    strideSELECT: integer
        stride length for `SELECT`.

    offsetSELECT: integer
        starting index for `SELECT`.

    N: integer
        number of columns.

    T: Float64Array
        `T`.

    strideT1: integer
        stride of dimension 1 of `T`.

    strideT2: integer
        stride of dimension 2 of `T`.

    offsetT: integer
        starting index for `T`.

    Q: Float64Array
        `Q`.

    strideQ1: integer
        stride of dimension 1 of `Q`.

    strideQ2: integer
        stride of dimension 2 of `Q`.

    offsetQ: integer
        starting index for `Q`.

    WR: Float64Array
        `WR`.

    strideWR: integer
        stride length for `WR`.

    offsetWR: integer
        starting index for `WR`.

    WI: Float64Array
        `WI`.

    strideWI: integer
        stride length for `WI`.

    offsetWI: integer
        starting index for `WI`.

    M: integer
        number of rows.

    s: number
        `s`.

    sep: number
        `sep`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    lwork: integer
        `lwork`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    offsetIWORK: integer
        starting index for `IWORK`.

    liwork: number
        `liwork`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
