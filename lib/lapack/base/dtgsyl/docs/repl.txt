{{alias}}( trans, ijob, M, N, A, LDA, B, LDB, C, LDC, D, LDD, E, LDE, F, LDF, scale, dif, WORK, strideWORK, lwork, IWORK, strideIWORK )
    Solves the generalized Sylvester equation (blocked):.

    Parameters
    ----------
    trans: string
        specifies whether the matrix should be transposed.

    ijob: number
        `ijob`.

    M: integer
        number of rows.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    B: Float64Array
        `B`.

    LDB: integer
        leading dimension of `B`.

    C: Float64Array
        `C`.

    LDC: integer
        leading dimension of `C`.

    D: Float64Array
        `D`.

    LDD: integer
        leading dimension of `D`.

    E: Float64Array
        `E`.

    LDE: integer
        leading dimension of `E`.

    F: Float64Array
        `F`.

    LDF: integer
        leading dimension of `F`.

    scale: number
        `scale`.

    dif: number
        `dif`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    lwork: integer
        `lwork`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( trans, ijob, M, N, A, strideA1, strideA2, offsetA, B, strideB1, strideB2, offsetB, C, strideC1, strideC2, offsetC, D, strideD1, strideD2, offsetD, E, strideE1, strideE2, offsetE, F, strideF1, strideF2, offsetF, scale, dif, WORK, strideWORK, offsetWORK, lwork, IWORK, strideIWORK, offsetIWORK )
    Solves the generalized Sylvester equation (blocked): using alternative
    indexing semantics.

    Parameters
    ----------
    trans: string
        specifies whether the matrix should be transposed.

    ijob: number
        `ijob`.

    M: integer
        number of rows.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    B: Float64Array
        `B`.

    strideB1: integer
        stride of dimension 1 of `B`.

    strideB2: integer
        stride of dimension 2 of `B`.

    offsetB: integer
        starting index for `B`.

    C: Float64Array
        `C`.

    strideC1: integer
        stride of dimension 1 of `C`.

    strideC2: integer
        stride of dimension 2 of `C`.

    offsetC: integer
        starting index for `C`.

    D: Float64Array
        `D`.

    strideD1: integer
        stride of dimension 1 of `D`.

    strideD2: integer
        stride of dimension 2 of `D`.

    offsetD: integer
        starting index for `D`.

    E: Float64Array
        `E`.

    strideE1: integer
        stride of dimension 1 of `E`.

    strideE2: integer
        stride of dimension 2 of `E`.

    offsetE: integer
        starting index for `E`.

    F: Float64Array
        `F`.

    strideF1: integer
        stride of dimension 1 of `F`.

    strideF2: integer
        stride of dimension 2 of `F`.

    offsetF: integer
        starting index for `F`.

    scale: number
        `scale`.

    dif: number
        `dif`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    lwork: integer
        `lwork`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    offsetIWORK: integer
        starting index for `IWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
