{{alias}}( M, N, kl, ku, AB, LDAB, r, strideR, c, strideC )
    Computes row and column scalings to equilibrate a real general band
    matrix.

    Parameters
    ----------
    M: integer
        number of rows.

    N: integer
        number of columns.

    kl: integer
        `kl`.

    ku: integer
        `ku`.

    AB: Float64Array
        `AB`.

    LDAB: integer
        leading dimension of `AB`.

    r: Float64Array
        `r`.

    strideR: integer
        stride length for `R`.

    c: Float64Array
        `c`.

    strideC: integer
        stride length for `C`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( M, N, kl, ku, AB, strideAB1, strideAB2, offsetAB, r, strideR, offsetR, c, strideC, offsetC )
    Computes row and column scalings to equilibrate a real general band
    matrix using alternative indexing semantics.

    Parameters
    ----------
    M: integer
        number of rows.

    N: integer
        number of columns.

    kl: integer
        `kl`.

    ku: integer
        `ku`.

    AB: Float64Array
        `AB`.

    strideAB1: integer
        stride of dimension 1 of `AB`.

    strideAB2: integer
        stride of dimension 2 of `AB`.

    offsetAB: integer
        starting index for `AB`.

    r: Float64Array
        `r`.

    strideR: integer
        stride length for `R`.

    offsetR: integer
        starting index for `R`.

    c: Float64Array
        `c`.

    strideC: integer
        stride length for `C`.

    offsetC: integer
        starting index for `C`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
