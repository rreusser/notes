

'use strict';

// MODULES //

var base = require( './base.js' );


// MAIN //

/**
* @license Apache-2.0
*
* @param {*} v - v
* @param {*} idx - idx
* @returns {Array} [re, im]
*/
function zlaqr5( v, idx ) {
	return base( v, idx );
}


// EXPORTS //

module.exports = zlaqr5;
