
{{alias}}( order, M, N, mb, nb, A, LDA, T, LDT, WORK, strideWORK )
    Generates an M-by-N complex matrix Q with orthonormal columns from a Tall-Skinny QR factorization (output of zlatsqr).

    Parameters
    ----------
        order: string
            Row-major (C-style) or column-major (Fortran-style) order.

        M: integer
            number of rows.

        N: integer
            number of columns.

        mb: integer
            mb.

        nb: integer
            nb.

        A: Float64Array
            input matrix.

        LDA: integer
            leading dimension of `A`.

        T: Float64Array
            input matrix.

        LDT: integer
            leading dimension of `T`.

        WORK: Float64Array
            output array.

        strideWORK: integer
            stride length for `WORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( M, N, mb, nb, A, strideA1, strideA2, offsetA, T, strideT1, strideT2, offsetT, WORK, strideWORK, offsetWORK )
    Generates an M-by-N complex matrix Q with orthonormal columns from a Tall-Skinny QR factorization (output of zlatsqr)., using alternative indexing semantics.

    Parameters
    ----------
        M: integer
            number of rows.

        N: integer
            number of columns.

        mb: integer
            mb.

        nb: integer
            nb.

        A: Float64Array
            input matrix.

        strideA1: integer
            stride of dimension 1 of `A`.

        strideA2: integer
            stride of dimension 2 of `A`.

        offsetA: integer
            starting index for `A`.

        T: Float64Array
            input matrix.

        strideT1: integer
            stride of dimension 1 of `T`.

        strideT2: integer
            stride of dimension 2 of `T`.

        offsetT: integer
            starting index for `T`.

        WORK: Float64Array
            output array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: integer
            starting index for `WORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
