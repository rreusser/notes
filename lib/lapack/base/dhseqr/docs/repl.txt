{{alias}}( job, compz, N, ilo, ihi, H, LDH, WR, strideWR, WI, strideWI, Z, LDZ )
    Computes the eigenvalues of a real upper Hessenberg matrix H, and.

    Parameters
    ----------
    job: string
        `job`.

    compz: string
        `compz`.

    N: integer
        number of columns.

    ilo: integer
        `ilo`.

    ihi: integer
        `ihi`.

    H: Float64Array
        `H`.

    LDH: integer
        leading dimension of `H`.

    WR: Float64Array
        `WR`.

    strideWR: integer
        stride length for `WR`.

    WI: Float64Array
        `WI`.

    strideWI: integer
        stride length for `WI`.

    Z: Float64Array
        `Z`.

    LDZ: integer
        leading dimension of `Z`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( job, compz, N, ilo, ihi, H, strideH1, strideH2, offsetH, WR, strideWR, offsetWR, WI, strideWI, offsetWI, Z, strideZ1, strideZ2, offsetZ, WORK, strideWORK, offsetWORK, lwork )
    Computes the eigenvalues of a real upper Hessenberg matrix H, and using
    alternative indexing semantics.

    Parameters
    ----------
    job: string
        `job`.

    compz: string
        `compz`.

    N: integer
        number of columns.

    ilo: integer
        `ilo`.

    ihi: integer
        `ihi`.

    H: Float64Array
        `H`.

    strideH1: integer
        stride of dimension 1 of `H`.

    strideH2: integer
        stride of dimension 2 of `H`.

    offsetH: integer
        starting index for `H`.

    WR: Float64Array
        `WR`.

    strideWR: integer
        stride length for `WR`.

    offsetWR: integer
        starting index for `WR`.

    WI: Float64Array
        `WI`.

    strideWI: integer
        stride length for `WI`.

    offsetWI: integer
        starting index for `WI`.

    Z: Float64Array
        `Z`.

    strideZ1: integer
        stride of dimension 1 of `Z`.

    strideZ2: integer
        stride of dimension 2 of `Z`.

    offsetZ: integer
        starting index for `Z`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    lwork: integer
        `lwork`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
