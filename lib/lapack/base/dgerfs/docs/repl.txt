{{alias}}( trans, N, nrhs, A, LDA, AF, LDAF, IPIV, strideIPIV, B, LDB, X, LDX, FERR, strideFERR, BERR, strideBERR )
    Improves the computed solution to a system of linear equations and
    provides.

    Parameters
    ----------
    trans: string
        specifies whether the matrix should be transposed.

    N: integer
        number of columns.

    nrhs: integer
        number of right-hand sides.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    AF: Float64Array
        `AF`.

    LDAF: integer
        leading dimension of `AF`.

    IPIV: Int32Array
        `IPIV`.

    strideIPIV: integer
        stride length for `IPIV`.

    B: Float64Array
        `B`.

    LDB: integer
        leading dimension of `B`.

    X: Float64Array
        `X`.

    LDX: integer
        leading dimension of `X`.

    FERR: Float64Array
        `FERR`.

    strideFERR: integer
        stride length for `FERR`.

    BERR: Float64Array
        `BERR`.

    strideBERR: integer
        stride length for `BERR`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( trans, N, nrhs, A, strideA1, strideA2, offsetA, AF, strideAF1, strideAF2, offsetAF, IPIV, strideIPIV, offsetIPIV, B, strideB1, strideB2, offsetB, X, strideX1, strideX2, offsetX, FERR, strideFERR, offsetFERR, BERR, strideBERR, offsetBERR )
    Improves the computed solution to a system of linear equations and
    provides using alternative indexing semantics.

    Parameters
    ----------
    trans: string
        specifies whether the matrix should be transposed.

    N: integer
        number of columns.

    nrhs: integer
        number of right-hand sides.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    AF: Float64Array
        `AF`.

    strideAF1: integer
        stride of dimension 1 of `AF`.

    strideAF2: integer
        stride of dimension 2 of `AF`.

    offsetAF: integer
        starting index for `AF`.

    IPIV: Int32Array
        `IPIV`.

    strideIPIV: integer
        stride length for `IPIV`.

    offsetIPIV: integer
        starting index for `IPIV`.

    B: Float64Array
        `B`.

    strideB1: integer
        stride of dimension 1 of `B`.

    strideB2: integer
        stride of dimension 2 of `B`.

    offsetB: integer
        starting index for `B`.

    X: Float64Array
        `X`.

    strideX1: integer
        stride of dimension 1 of `X`.

    strideX2: integer
        stride of dimension 2 of `X`.

    offsetX: integer
        starting index for `X`.

    FERR: Float64Array
        `FERR`.

    strideFERR: integer
        stride length for `FERR`.

    offsetFERR: integer
        starting index for `FERR`.

    BERR: Float64Array
        `BERR`.

    strideBERR: integer
        stride length for `BERR`.

    offsetBERR: integer
        starting index for `BERR`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
