
{{alias}}( order, trans, M, N, kl, ku, alpha, AB, LDAB, x, strideX, beta, y, strideY )
    Performs a matrix-vector operation to calculate error bounds on banded matrices

    Parameters
    ----------
        order: string
            Row-major (C-style) or column-major (Fortran-style) order.

        trans: integer
            trans.

        M: integer
            number of rows.

        N: integer
            number of columns.

        kl: integer
            kl.

        ku: integer
            ku.

        alpha: number
            scalar constant.

        AB: Float64Array
            input matrix.

        LDAB: integer
            leading dimension of `AB`.

        x: Float64Array
            input array.

        strideX: integer
            stride length for `x`.

        beta: number
            scalar constant.

        y: Float64Array
            output array.

        strideY: integer
            stride length for `y`.

    Returns
    -------
    out: None
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( trans, M, N, kl, ku, alpha, AB, strideAB1, strideAB2, offsetAB, x, strideX, offsetX, beta, y, strideY, offsetY )
    Performs a matrix-vector operation to calculate error bounds on banded matrices, using alternative indexing semantics.

    Parameters
    ----------
        trans: integer
            trans.

        M: integer
            number of rows.

        N: integer
            number of columns.

        kl: integer
            kl.

        ku: integer
            ku.

        alpha: number
            scalar constant.

        AB: Float64Array
            input matrix.

        strideAB1: integer
            stride of dimension 1 of `AB`.

        strideAB2: integer
            stride of dimension 2 of `AB`.

        offsetAB: integer
            starting index for `AB`.

        x: Float64Array
            input array.

        strideX: integer
            stride length for `x`.

        offsetX: integer
            starting index for `X`.

        beta: number
            scalar constant.

        y: Float64Array
            output array.

        strideY: integer
            stride length for `y`.

        offsetY: integer
            starting index for `Y`.

    Returns
    -------
    out: None
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
