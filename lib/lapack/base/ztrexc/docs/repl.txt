{{alias}}( compq, N, T, LDT, Q, LDQ, ifst, ilst )
    Reorders the Schur factorization of a complex matrix A = Q*T*Q^H, so
    that.

    Parameters
    ----------
    compq: string
        `compq`.

    N: integer
        number of columns.

    T: Float64Array
        `T`.

    LDT: integer
        leading dimension of `T`.

    Q: Float64Array
        `Q`.

    LDQ: integer
        leading dimension of `Q`.

    ifst: number
        `ifst`.

    ilst: number
        `ilst`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( compq, N, T, strideT1, strideT2, offsetT, Q, strideQ1, strideQ2, offsetQ, ifst, ilst )
    Reorders the Schur factorization of a complex matrix A = Q*T*Q^H, so
    that using alternative indexing semantics.

    Parameters
    ----------
    compq: string
        `compq`.

    N: integer
        number of columns.

    T: Float64Array
        `T`.

    strideT1: integer
        stride of dimension 1 of `T`.

    strideT2: integer
        stride of dimension 2 of `T`.

    offsetT: integer
        starting index for `T`.

    Q: Float64Array
        `Q`.

    strideQ1: integer
        stride of dimension 1 of `Q`.

    strideQ2: integer
        stride of dimension 2 of `Q`.

    offsetQ: integer
        starting index for `Q`.

    ifst: number
        `ifst`.

    ilst: number
        `ilst`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
