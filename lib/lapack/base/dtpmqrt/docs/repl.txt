
{{alias}}( order, side, trans, M, N, K, l, nb, V, LDV, T, LDT, A, LDA, B, LDB, WORK, strideWORK )
    Apply Q from a triangular-pentagonal QR factorization (output of dtpqrt) to a stacked matrix C composed of A and B blocks

    Parameters
    ----------
        order: string
            Row-major (C-style) or column-major (Fortran-style) order.

        side: string
            specifies the operation type.

        trans: string
            specifies the operation type.

        M: integer
            number of rows.

        N: integer
            number of columns.

        K: integer
            number of superdiagonals.

        l: integer
            l.

        nb: integer
            nb.

        V: Float64Array
            input matrix.

        LDV: integer
            leading dimension of `V`.

        T: Float64Array
            input matrix.

        LDT: integer
            leading dimension of `T`.

        A: Float64Array
            input matrix.

        LDA: integer
            leading dimension of `A`.

        B: Float64Array
            input matrix.

        LDB: integer
            leading dimension of `B`.

        WORK: Float64Array
            output array.

        strideWORK: integer
            stride length for `WORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( side, trans, M, N, K, l, nb, V, strideV1, strideV2, offsetV, T, strideT1, strideT2, offsetT, A, strideA1, strideA2, offsetA, B, strideB1, strideB2, offsetB, WORK, strideWORK, offsetWORK )
    Apply Q from a triangular-pentagonal QR factorization (output of dtpqrt) to a stacked matrix C composed of A and B blocks, using alternative indexing semantics.

    Parameters
    ----------
        side: string
            specifies the operation type.

        trans: string
            specifies the operation type.

        M: integer
            number of rows.

        N: integer
            number of columns.

        K: integer
            number of superdiagonals.

        l: integer
            l.

        nb: integer
            nb.

        V: Float64Array
            input matrix.

        strideV1: integer
            stride of dimension 1 of `V`.

        strideV2: integer
            stride of dimension 2 of `V`.

        offsetV: integer
            starting index for `V`.

        T: Float64Array
            input matrix.

        strideT1: integer
            stride of dimension 1 of `T`.

        strideT2: integer
            stride of dimension 2 of `T`.

        offsetT: integer
            starting index for `T`.

        A: Float64Array
            input matrix.

        strideA1: integer
            stride of dimension 1 of `A`.

        strideA2: integer
            stride of dimension 2 of `A`.

        offsetA: integer
            starting index for `A`.

        B: Float64Array
            input matrix.

        strideB1: integer
            stride of dimension 1 of `B`.

        strideB2: integer
            stride of dimension 2 of `B`.

        offsetB: integer
            starting index for `B`.

        WORK: Float64Array
            output array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: integer
            starting index for `WORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
