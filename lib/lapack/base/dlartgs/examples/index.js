'use strict';

var dlartgs = require( './../lib' );

var result = dlartgs( 3.0, 4.0, 1.5 );
console.log( result ); // eslint-disable-line no-console

result = dlartgs( -2.0, 3.0, 0.0 );
console.log( result ); // eslint-disable-line no-console
