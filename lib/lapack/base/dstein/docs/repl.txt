{{alias}}( N, d, strideD, e, strideE, M, w, strideW, IBLOCK, strideIBLOCK, ISPLIT, strideISPLIT, Z, LDZ, WORK, strideWORK, IWORK, strideIWORK, IFAIL, strideIFAIL )
    Computes the eigenvectors of a real symmetric tridiagonal matrix T.

    Parameters
    ----------
    N: integer
        number of columns.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    e: Float64Array
        `e`.

    strideE: integer
        stride length for `E`.

    M: integer
        number of rows.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    IBLOCK: Int32Array
        `IBLOCK`.

    strideIBLOCK: integer
        stride length for `IBLOCK`.

    ISPLIT: Int32Array
        `ISPLIT`.

    strideISPLIT: integer
        stride length for `ISPLIT`.

    Z: Float64Array
        `Z`.

    LDZ: integer
        leading dimension of `Z`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    IFAIL: Float64Array
        `IFAIL`.

    strideIFAIL: integer
        stride length for `IFAIL`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, d, strideD, offsetD, e, strideE, offsetE, M, w, strideW, offsetW, IBLOCK, strideIBLOCK, offsetIBLOCK, ISPLIT, strideISPLIT, offsetISPLIT, Z, strideZ1, strideZ2, offsetZ, WORK, strideWORK, offsetWORK, IWORK, strideIWORK, offsetIWORK, IFAIL, strideIFAIL, offsetIFAIL )
    Computes the eigenvectors of a real symmetric tridiagonal matrix T using
    alternative indexing semantics.

    Parameters
    ----------
    N: integer
        number of columns.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    offsetD: integer
        starting index for `D`.

    e: Float64Array
        `e`.

    strideE: integer
        stride length for `E`.

    offsetE: integer
        starting index for `E`.

    M: integer
        number of rows.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    offsetW: integer
        starting index for `W`.

    IBLOCK: Int32Array
        `IBLOCK`.

    strideIBLOCK: integer
        stride length for `IBLOCK`.

    offsetIBLOCK: integer
        starting index for `IBLOCK`.

    ISPLIT: Int32Array
        `ISPLIT`.

    strideISPLIT: integer
        stride length for `ISPLIT`.

    offsetISPLIT: integer
        starting index for `ISPLIT`.

    Z: Float64Array
        `Z`.

    strideZ1: integer
        stride of dimension 1 of `Z`.

    strideZ2: integer
        stride of dimension 2 of `Z`.

    offsetZ: integer
        starting index for `Z`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    offsetIWORK: integer
        starting index for `IWORK`.

    IFAIL: Float64Array
        `IFAIL`.

    strideIFAIL: integer
        stride length for `IFAIL`.

    offsetIFAIL: integer
        starting index for `IFAIL`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
