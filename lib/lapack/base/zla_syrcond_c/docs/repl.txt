
{{alias}}( order, uplo, N, A, LDA, AF, LDAF, IPIV, strideIPIV, offsetIPIV, c, strideC, capply, WORK, strideWORK, RWORK, strideRWORK )
    Estimates the infinity norm condition number for a complex symmetric indefinite matrix with inverse-c scaling

    Parameters
    ----------
        order: string
            Row-major (C-style) or column-major (Fortran-style) order.

        uplo: string
            specifies the operation type.

        N: integer
            number of columns.

        A: Float64Array
            input matrix.

        LDA: integer
            leading dimension of `A`.

        AF: Float64Array
            input matrix.

        LDAF: integer
            leading dimension of `AF`.

        IPIV: Int32Array
            input array.

        strideIPIV: integer
            stride length for `IPIV`.

        offsetIPIV: integer
            starting index for `IPIV`.

        c: Float64Array
            input array.

        strideC: integer
            stride length for `c`.

        capply: boolean
            capply.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        RWORK: Float64Array
            output array.

        strideRWORK: integer
            stride length for `RWORK`.

    Returns
    -------
    out: number
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( uplo, N, A, strideA1, strideA2, offsetA, AF, strideAF1, strideAF2, offsetAF, IPIV, strideIPIV, offsetIPIV, c, strideC, offsetC, capply, WORK, strideWORK, offsetWORK, RWORK, strideRWORK, offsetRWORK )
    Estimates the infinity norm condition number for a complex symmetric indefinite matrix with inverse-c scaling, using alternative indexing semantics.

    Parameters
    ----------
        uplo: string
            specifies the operation type.

        N: integer
            number of columns.

        A: Float64Array
            input matrix.

        strideA1: integer
            stride of dimension 1 of `A`.

        strideA2: integer
            stride of dimension 2 of `A`.

        offsetA: integer
            starting index for `A`.

        AF: Float64Array
            input matrix.

        strideAF1: integer
            stride of dimension 1 of `AF`.

        strideAF2: integer
            stride of dimension 2 of `AF`.

        offsetAF: integer
            starting index for `AF`.

        IPIV: Int32Array
            input array.

        strideIPIV: integer
            stride length for `IPIV`.

        offsetIPIV: integer
            starting index for `IPIV`.

        c: Float64Array
            input array.

        strideC: integer
            stride length for `c`.

        offsetC: integer
            starting index for `C`.

        capply: boolean
            capply.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: integer
            starting index for `WORK`.

        RWORK: Float64Array
            output array.

        strideRWORK: integer
            stride length for `RWORK`.

        offsetRWORK: integer
            starting index for `RWORK`.

    Returns
    -------
    out: number
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
