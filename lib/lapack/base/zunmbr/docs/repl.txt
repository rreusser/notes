{{alias}}( order, vect, side, trans, M, N, K, A, LDA, TAU, strideTAU, C, LDC, WORK, strideWORK )
    If VECT = 'Q', overwrite the matrix C with one of:.

    Parameters
    ----------
    order: string
        Row-major (C-style) or column-major (Fortran-style) order.

    vect: string
        `vect`.

    side: string
        specifies the side of the operation.

    trans: string
        specifies whether the matrix should be transposed.

    M: integer
        number of rows.

    N: integer
        number of columns.

    K: integer
        inner dimension.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    TAU: Float64Array
        `TAU`.

    strideTAU: integer
        stride length for `TAU`.

    C: Float64Array
        `C`.

    LDC: integer
        leading dimension of `C`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( vect, side, trans, M, N, K, A, strideA1, strideA2, offsetA, TAU, strideTAU, offsetTAU, C, strideC1, strideC2, offsetC, WORK, strideWORK, offsetWORK )
    If VECT = 'Q', overwrite the matrix C with one of: using alternative
    indexing semantics.

    Parameters
    ----------
    vect: string
        `vect`.

    side: string
        specifies the side of the operation.

    trans: string
        specifies whether the matrix should be transposed.

    M: integer
        number of rows.

    N: integer
        number of columns.

    K: integer
        inner dimension.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    TAU: Float64Array
        `TAU`.

    strideTAU: integer
        stride length for `TAU`.

    offsetTAU: integer
        starting index for `TAU`.

    C: Float64Array
        `C`.

    strideC1: integer
        stride of dimension 1 of `C`.

    strideC2: integer
        stride of dimension 2 of `C`.

    offsetC: integer
        starting index for `C`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
