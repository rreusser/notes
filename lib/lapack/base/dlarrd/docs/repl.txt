
{{alias}}( range, order, N, vl, vu, il, iu, GERS, strideGERS, reltol, d, strideD, e, strideE, E2, strideE2, pivmin, nsplit, ISPLIT, strideISPLIT, offsetISPLIT, M, w, strideW, WERR, strideWERR, wl, wu, IBLOCK, strideIBLOCK, offsetIBLOCK, INDEXW, strideINDEXW, offsetINDEXW, WORK, strideWORK, IWORK, strideIWORK, offsetIWORK )
    Computes eigenvalues of a symmetric tridiagonal matrix to suitable accuracy

    Parameters
    ----------
        range: string
            specifies the operation type.

        order: string
            Row-major (C-style) or column-major (Fortran-style) order.

        N: integer
            number of columns.

        vl: number
            vl.

        vu: number
            vu.

        il: integer
            il.

        iu: integer
            iu.

        GERS: Float64Array
            input array.

        strideGERS: integer
            stride length for `GERS`.

        reltol: number
            reltol.

        d: Float64Array
            input array.

        strideD: integer
            stride length for `d`.

        e: Float64Array
            input array.

        strideE: integer
            stride length for `e`.

        E2: Float64Array
            input array.

        strideE2: integer
            stride length for `E2`.

        pivmin: number
            pivmin.

        nsplit: integer
            nsplit.

        ISPLIT: Int32Array
            input array.

        strideISPLIT: integer
            stride length for `ISPLIT`.

        offsetISPLIT: integer
            starting index for `ISPLIT`.

        M: integer
            number of rows.

        w: Float64Array
            input array.

        strideW: integer
            stride length for `w`.

        WERR: Float64Array
            input array.

        strideWERR: integer
            stride length for `WERR`.

        wl: number
            wl.

        wu: number
            wu.

        IBLOCK: Int32Array
            input array.

        strideIBLOCK: integer
            stride length for `IBLOCK`.

        offsetIBLOCK: integer
            starting index for `IBLOCK`.

        INDEXW: Int32Array
            input array.

        strideINDEXW: integer
            stride length for `INDEXW`.

        offsetINDEXW: integer
            starting index for `INDEXW`.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        IWORK: Int32Array
            output array.

        strideIWORK: integer
            stride length for `IWORK`.

        offsetIWORK: integer
            starting index for `IWORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( range, order, N, vl, vu, il, iu, GERS, strideGERS, offsetGERS, reltol, d, strideD, offsetD, e, strideE, offsetE, E2, strideE2, offsetE2, pivmin, nsplit, ISPLIT, strideISPLIT, offsetISPLIT, M, w, strideW, offsetW, WERR, strideWERR, offsetWERR, wl, wu, IBLOCK, strideIBLOCK, offsetIBLOCK, INDEXW, strideINDEXW, offsetINDEXW, WORK, strideWORK, offsetWORK, IWORK, strideIWORK, offsetIWORK )
    Computes eigenvalues of a symmetric tridiagonal matrix to suitable accuracy, using alternative indexing semantics.

    Parameters
    ----------
        range: string
            specifies the operation type.

        order: string
            specifies the operation type.

        N: integer
            number of columns.

        vl: number
            vl.

        vu: number
            vu.

        il: integer
            il.

        iu: integer
            iu.

        GERS: Float64Array
            input array.

        strideGERS: integer
            stride length for `GERS`.

        offsetGERS: integer
            starting index for `GERS`.

        reltol: number
            reltol.

        d: Float64Array
            input array.

        strideD: integer
            stride length for `d`.

        offsetD: integer
            starting index for `D`.

        e: Float64Array
            input array.

        strideE: integer
            stride length for `e`.

        offsetE: integer
            starting index for `E`.

        E2: Float64Array
            input array.

        strideE2: integer
            stride of dimension 2 of `E`.

        offsetE2: integer
            starting index for `E2`.

        pivmin: number
            pivmin.

        nsplit: integer
            nsplit.

        ISPLIT: Int32Array
            input array.

        strideISPLIT: integer
            stride length for `ISPLIT`.

        offsetISPLIT: integer
            starting index for `ISPLIT`.

        M: integer
            number of rows.

        w: Float64Array
            input array.

        strideW: integer
            stride length for `w`.

        offsetW: integer
            starting index for `W`.

        WERR: Float64Array
            input array.

        strideWERR: integer
            stride length for `WERR`.

        offsetWERR: integer
            starting index for `WERR`.

        wl: number
            wl.

        wu: number
            wu.

        IBLOCK: Int32Array
            input array.

        strideIBLOCK: integer
            stride length for `IBLOCK`.

        offsetIBLOCK: integer
            starting index for `IBLOCK`.

        INDEXW: Int32Array
            input array.

        strideINDEXW: integer
            stride length for `INDEXW`.

        offsetINDEXW: integer
            starting index for `INDEXW`.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: integer
            starting index for `WORK`.

        IWORK: Int32Array
            output array.

        strideIWORK: integer
            stride length for `IWORK`.

        offsetIWORK: integer
            starting index for `IWORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
