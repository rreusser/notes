{{alias}}( range, order, N, vl, vu, il, iu, abstol, d, strideD, e, strideE, M, nsplit, w, strideW, IBLOCK, strideIBLOCK, ISPLIT, strideISPLIT, WORK, strideWORK, IWORK, strideIWORK )
    Computes selected eigenvalues of a real symmetric tridiagonal matrix T.

    Parameters
    ----------
    range: string
        `range`.

    order: string
        Row-major (C-style) or column-major (Fortran-style) order.

    N: integer
        number of columns.

    vl: number
        `vl`.

    vu: number
        `vu`.

    il: integer
        `il`.

    iu: integer
        `iu`.

    abstol: number
        `abstol`.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    e: Float64Array
        `e`.

    strideE: integer
        stride length for `E`.

    M: integer
        number of rows.

    nsplit: number
        `nsplit`.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    IBLOCK: Int32Array
        `IBLOCK`.

    strideIBLOCK: integer
        stride length for `IBLOCK`.

    ISPLIT: Int32Array
        `ISPLIT`.

    strideISPLIT: integer
        stride length for `ISPLIT`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( range, order, N, vl, vu, il, iu, abstol, d, strideD, offsetD, e, strideE, offsetE, M, nsplit, w, strideW, offsetW, IBLOCK, strideIBLOCK, offsetIBLOCK, ISPLIT, strideISPLIT, offsetISPLIT, WORK, strideWORK, offsetWORK, IWORK, strideIWORK, offsetIWORK )
    Computes selected eigenvalues of a real symmetric tridiagonal matrix T
    using alternative indexing semantics.

    Parameters
    ----------
    range: string
        `range`.

    order: string
        Row-major (C-style) or column-major (Fortran-style) order.

    N: integer
        number of columns.

    vl: number
        `vl`.

    vu: number
        `vu`.

    il: integer
        `il`.

    iu: integer
        `iu`.

    abstol: number
        `abstol`.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    offsetD: integer
        starting index for `D`.

    e: Float64Array
        `e`.

    strideE: integer
        stride length for `E`.

    offsetE: integer
        starting index for `E`.

    M: integer
        number of rows.

    nsplit: number
        `nsplit`.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    offsetW: integer
        starting index for `W`.

    IBLOCK: Int32Array
        `IBLOCK`.

    strideIBLOCK: integer
        stride length for `IBLOCK`.

    offsetIBLOCK: integer
        starting index for `IBLOCK`.

    ISPLIT: Int32Array
        `ISPLIT`.

    strideISPLIT: integer
        stride length for `ISPLIT`.

    offsetISPLIT: integer
        starting index for `ISPLIT`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    offsetIWORK: integer
        starting index for `IWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
