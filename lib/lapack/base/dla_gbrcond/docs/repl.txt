
{{alias}}( order, trans, N, kl, ku, AB, LDAB, AFB, LDAFB, IPIV, strideIPIV, offsetIPIV, cmode, c, strideC, WORK, strideWORK, IWORK, strideIWORK, offsetIWORK )
    Estimates the Skeel condition number for a general banded matrix

    Parameters
    ----------
        order: string
            Row-major (C-style) or column-major (Fortran-style) order.

        trans: string
            specifies the operation type.

        N: integer
            number of columns.

        kl: integer
            kl.

        ku: integer
            ku.

        AB: Float64Array
            input matrix.

        LDAB: integer
            leading dimension of `AB`.

        AFB: Float64Array
            input matrix.

        LDAFB: integer
            leading dimension of `AFB`.

        IPIV: Int32Array
            input array.

        strideIPIV: integer
            stride length for `IPIV`.

        offsetIPIV: integer
            starting index for `IPIV`.

        cmode: integer
            cmode.

        c: Float64Array
            input array.

        strideC: integer
            stride length for `c`.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        IWORK: Int32Array
            output array.

        strideIWORK: integer
            stride length for `IWORK`.

        offsetIWORK: integer
            starting index for `IWORK`.

    Returns
    -------
    out: number
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( trans, N, kl, ku, AB, strideAB1, strideAB2, offsetAB, AFB, strideAFB1, strideAFB2, offsetAFB, IPIV, strideIPIV, offsetIPIV, cmode, c, strideC, offsetC, WORK, strideWORK, offsetWORK, IWORK, strideIWORK, offsetIWORK )
    Estimates the Skeel condition number for a general banded matrix, using alternative indexing semantics.

    Parameters
    ----------
        trans: string
            specifies the operation type.

        N: integer
            number of columns.

        kl: integer
            kl.

        ku: integer
            ku.

        AB: Float64Array
            input matrix.

        strideAB1: integer
            stride of dimension 1 of `AB`.

        strideAB2: integer
            stride of dimension 2 of `AB`.

        offsetAB: integer
            starting index for `AB`.

        AFB: Float64Array
            input matrix.

        strideAFB1: integer
            stride of dimension 1 of `AFB`.

        strideAFB2: integer
            stride of dimension 2 of `AFB`.

        offsetAFB: integer
            starting index for `AFB`.

        IPIV: Int32Array
            input array.

        strideIPIV: integer
            stride length for `IPIV`.

        offsetIPIV: integer
            starting index for `IPIV`.

        cmode: integer
            cmode.

        c: Float64Array
            input array.

        strideC: integer
            stride length for `c`.

        offsetC: integer
            starting index for `C`.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: integer
            starting index for `WORK`.

        IWORK: Int32Array
            output array.

        strideIWORK: integer
            stride length for `IWORK`.

        offsetIWORK: integer
            starting index for `IWORK`.

    Returns
    -------
    out: number
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
