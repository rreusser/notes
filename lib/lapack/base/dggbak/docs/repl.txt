{{alias}}( order, job, side, N, ilo, ihi, LSCALE, strideLSCALE, RSCALE, strideRSCALE, M, V, LDV )
    @license Apache-2.0.

    Parameters
    ----------
    order: string
        Row-major (C-style) or column-major (Fortran-style) order.

    job: string
        `job`.

    side: string
        specifies the side of the operation.

    N: integer
        number of columns.

    ilo: integer
        `ilo`.

    ihi: integer
        `ihi`.

    LSCALE: Float64Array
        `LSCALE`.

    strideLSCALE: integer
        stride length for `LSCALE`.

    RSCALE: Float64Array
        `RSCALE`.

    strideRSCALE: integer
        stride length for `RSCALE`.

    M: integer
        number of rows.

    V: Float64Array
        `V`.

    LDV: integer
        leading dimension of `V`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( job, side, N, ilo, ihi, LSCALE, strideLSCALE, offsetLSCALE, RSCALE, strideRSCALE, offsetRSCALE, M, V, strideV1, strideV2, offsetV )
    @license Apache-2.0 using alternative indexing semantics.

    Parameters
    ----------
    job: string
        `job`.

    side: string
        specifies the side of the operation.

    N: integer
        number of columns.

    ilo: integer
        `ilo`.

    ihi: integer
        `ihi`.

    LSCALE: Float64Array
        `LSCALE`.

    strideLSCALE: integer
        stride length for `LSCALE`.

    offsetLSCALE: integer
        starting index for `LSCALE`.

    RSCALE: Float64Array
        `RSCALE`.

    strideRSCALE: integer
        stride length for `RSCALE`.

    offsetRSCALE: integer
        starting index for `RSCALE`.

    M: integer
        number of rows.

    V: Float64Array
        `V`.

    strideV1: integer
        stride of dimension 1 of `V`.

    strideV2: integer
        stride of dimension 2 of `V`.

    offsetV: integer
        starting index for `V`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
