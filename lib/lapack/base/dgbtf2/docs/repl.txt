{{alias}}( order, M, N, kl, ku, AB, LDAB, IPIV, strideIPIV )
    Computes an LU factorization of a real M-by-N band matrix A using
    partial.

    Parameters
    ----------
    order: string
        Row-major (C-style) or column-major (Fortran-style) order.

    M: integer
        number of rows.

    N: integer
        number of columns.

    kl: integer
        `kl`.

    ku: integer
        `ku`.

    AB: Float64Array
        `AB`.

    LDAB: integer
        leading dimension of `AB`.

    IPIV: Int32Array
        `IPIV`.

    strideIPIV: integer
        stride length for `IPIV`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( M, N, kl, ku, AB, strideAB1, strideAB2, offsetAB, IPIV, strideIPIV, offsetIPIV )
    Computes an LU factorization of a real M-by-N band matrix A using
    partial using alternative indexing semantics.

    Parameters
    ----------
    M: integer
        number of rows.

    N: integer
        number of columns.

    kl: integer
        `kl`.

    ku: integer
        `ku`.

    AB: Float64Array
        `AB`.

    strideAB1: integer
        stride of dimension 1 of `AB`.

    strideAB2: integer
        stride of dimension 2 of `AB`.

    offsetAB: integer
        starting index for `AB`.

    IPIV: Int32Array
        `IPIV`.

    strideIPIV: integer
        stride length for `IPIV`.

    offsetIPIV: integer
        starting index for `IPIV`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
