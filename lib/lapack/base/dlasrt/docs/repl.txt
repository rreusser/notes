{{alias}}( id, N, d, stride )
    Sort an array of doubles in increasing or decreasing order using
    quicksort.

    Parameters
    ----------
    id: number
        `id`.

    N: integer
        number of columns.

    d: number
        `d`.

    stride: integer
        `stride`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( id, N, d, stride, offset )
    Sort an array of doubles in increasing or decreasing order using
    quicksort using alternative indexing semantics.

    Parameters
    ----------
    id: number
        `id`.

    N: integer
        number of columns.

    d: number
        `d`.

    stride: integer
        `stride`.

    offset: integer
        `offset`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
