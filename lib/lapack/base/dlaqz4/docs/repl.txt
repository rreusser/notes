
{{alias}}( order, ilschur, ilq, ilz, N, ilo, ihi, nshifts, nblockDesired, SR, strideSR, SI, strideSI, SS, strideSS, A, LDA, B, LDB, Q, LDQ, Z, LDZ, QC, LDQC, ZC, LDZC, WORK, strideWORK )
    Executes a single multishift QZ sweep on a matrix pencil.

    Parameters
    ----------
        order: string
            Row-major (C-style) or column-major (Fortran-style) order.

        ilschur: boolean
            ilschur.

        ilq: boolean
            ilq.

        ilz: boolean
            ilz.

        N: integer
            number of columns.

        ilo: integer
            ilo.

        ihi: integer
            ihi.

        nshifts: integer
            nshifts.

        nblockDesired: integer
            nblockDesired.

        SR: Float64Array
            input array.

        strideSR: integer
            stride length for `SR`.

        SI: Float64Array
            input array.

        strideSI: integer
            stride length for `SI`.

        SS: Float64Array
            input array.

        strideSS: integer
            stride length for `SS`.

        A: Float64Array
            input matrix.

        LDA: integer
            leading dimension of `A`.

        B: Float64Array
            input matrix.

        LDB: integer
            leading dimension of `B`.

        Q: Float64Array
            input matrix.

        LDQ: integer
            leading dimension of `Q`.

        Z: Float64Array
            input matrix.

        LDZ: integer
            leading dimension of `Z`.

        QC: Float64Array
            input matrix.

        LDQC: integer
            leading dimension of `QC`.

        ZC: Float64Array
            input matrix.

        LDZC: integer
            leading dimension of `ZC`.

        WORK: Float64Array
            output array.

        strideWORK: integer
            stride length for `WORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( ilschur, ilq, ilz, N, ilo, ihi, nshifts, nblockDesired, SR, strideSR, offsetSR, SI, strideSI, offsetSI, SS, strideSS, offsetSS, A, strideA1, strideA2, offsetA, B, strideB1, strideB2, offsetB, Q, strideQ1, strideQ2, offsetQ, Z, strideZ1, strideZ2, offsetZ, QC, strideQC1, strideQC2, offsetQC, ZC, strideZC1, strideZC2, offsetZC, WORK, strideWORK, offsetWORK )
    Executes a single multishift QZ sweep on a matrix pencil., using alternative indexing semantics.

    Parameters
    ----------
        ilschur: boolean
            ilschur.

        ilq: boolean
            ilq.

        ilz: boolean
            ilz.

        N: integer
            number of columns.

        ilo: integer
            ilo.

        ihi: integer
            ihi.

        nshifts: integer
            nshifts.

        nblockDesired: integer
            nblockDesired.

        SR: Float64Array
            input array.

        strideSR: integer
            stride length for `SR`.

        offsetSR: integer
            starting index for `SR`.

        SI: Float64Array
            input array.

        strideSI: integer
            stride length for `SI`.

        offsetSI: integer
            starting index for `SI`.

        SS: Float64Array
            input array.

        strideSS: integer
            stride length for `SS`.

        offsetSS: integer
            starting index for `SS`.

        A: Float64Array
            input matrix.

        strideA1: integer
            stride of dimension 1 of `A`.

        strideA2: integer
            stride of dimension 2 of `A`.

        offsetA: integer
            starting index for `A`.

        B: Float64Array
            input matrix.

        strideB1: integer
            stride of dimension 1 of `B`.

        strideB2: integer
            stride of dimension 2 of `B`.

        offsetB: integer
            starting index for `B`.

        Q: Float64Array
            input matrix.

        strideQ1: integer
            stride of dimension 1 of `Q`.

        strideQ2: integer
            stride of dimension 2 of `Q`.

        offsetQ: integer
            starting index for `Q`.

        Z: Float64Array
            input matrix.

        strideZ1: integer
            stride of dimension 1 of `Z`.

        strideZ2: integer
            stride of dimension 2 of `Z`.

        offsetZ: integer
            starting index for `Z`.

        QC: Float64Array
            input matrix.

        strideQC1: integer
            stride of dimension 1 of `QC`.

        strideQC2: integer
            stride of dimension 2 of `QC`.

        offsetQC: integer
            starting index for `QC`.

        ZC: Float64Array
            input matrix.

        strideZC1: integer
            stride of dimension 1 of `ZC`.

        strideZC2: integer
            stride of dimension 2 of `ZC`.

        offsetZC: integer
            starting index for `ZC`.

        WORK: Float64Array
            output array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: integer
            starting index for `WORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
