{{alias}}( jobvs, sort, select, N, A, LDA, sdim, W, strideW, VS, LDVS, WORK, strideWORK, lwork, RWORK, strideRWORK, BWORK, strideBWORK )
    Computes for an N-by-N complex nonsymmetric matrix A, the eigenvalues.

    Parameters
    ----------
    jobvs: string
        `jobvs`.

    sort: string
        `sort`.

    select: number
        `select`.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    sdim: number
        `sdim`.

    W: Float64Array
        `W`.

    strideW: integer
        stride length for `W`.

    VS: Float64Array
        `VS`.

    LDVS: integer
        leading dimension of `VS`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    lwork: integer
        `lwork`.

    RWORK: Float64Array
        `RWORK`.

    strideRWORK: integer
        stride length for `RWORK`.

    BWORK: Int32Array
        `BWORK`.

    strideBWORK: integer
        stride length for `BWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( jobvs, sort, select, N, A, strideA1, strideA2, offsetA, sdim, W, strideW, offsetW, VS, strideVS1, strideVS2, offsetVS, WORK, strideWORK, offsetWORK, lwork, RWORK, strideRWORK, offsetRWORK, BWORK, strideBWORK, offsetBWORK )
    Computes for an N-by-N complex nonsymmetric matrix A, the eigenvalues,
    using alternative indexing semantics.

    Parameters
    ----------
    jobvs: string
        `jobvs`.

    sort: string
        `sort`.

    select: number
        `select`.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    sdim: number
        `sdim`.

    W: Float64Array
        `W`.

    strideW: integer
        stride length for `W`.

    offsetW: integer
        starting index for `W`.

    VS: Float64Array
        `VS`.

    strideVS1: integer
        stride of dimension 1 of `VS`.

    strideVS2: integer
        stride of dimension 2 of `VS`.

    offsetVS: integer
        starting index for `VS`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    lwork: integer
        `lwork`.

    RWORK: Float64Array
        `RWORK`.

    strideRWORK: integer
        stride length for `RWORK`.

    offsetRWORK: integer
        starting index for `RWORK`.

    BWORK: Int32Array
        `BWORK`.

    strideBWORK: integer
        stride length for `BWORK`.

    offsetBWORK: integer
        starting index for `BWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
