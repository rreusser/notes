{{alias}}( N, d, strideD, E2, strideE2, ifirst, ilast, rtol, offset, w, strideW, WERR, strideWERR, WORK, strideWORK, IWORK, strideIWORK, pivmin, spdiam )
    Refine eigenvalue approximations using bisection given initial
    intervals.

    Parameters
    ----------
    N: integer
        number of columns.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    E2: Float64Array
        `E2`.

    strideE2: integer
        stride of dimension 2 of `E`.

    ifirst: number
        `ifirst`.

    ilast: number
        `ilast`.

    rtol: number
        `rtol`.

    offset: integer
        `offset`.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    WERR: Float64Array
        `WERR`.

    strideWERR: integer
        stride length for `WERR`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    pivmin: number
        `pivmin`.

    spdiam: number
        `spdiam`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, d, strideD, offsetD, E2, strideE2, offsetE2, ifirst, ilast, rtol, offset, w, strideW, offsetW, WERR, strideWERR, offsetWERR, WORK, strideWORK, offsetWORK, IWORK, strideIWORK, offsetIWORK, pivmin, spdiam )
    Refine eigenvalue approximations using bisection given initial intervals
    using alternative indexing semantics.

    Parameters
    ----------
    N: integer
        number of columns.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    offsetD: integer
        starting index for `D`.

    E2: Float64Array
        `E2`.

    strideE2: integer
        stride of dimension 2 of `E`.

    offsetE2: integer
        starting index for `E2`.

    ifirst: number
        `ifirst`.

    ilast: number
        `ilast`.

    rtol: number
        `rtol`.

    offset: integer
        `offset`.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    offsetW: integer
        starting index for `W`.

    WERR: Float64Array
        `WERR`.

    strideWERR: integer
        stride length for `WERR`.

    offsetWERR: integer
        starting index for `WERR`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    offsetIWORK: integer
        starting index for `IWORK`.

    pivmin: number
        `pivmin`.

    spdiam: number
        `spdiam`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
