{{alias}}( v, idx )
    CABS1: |re(z)| + |im(z)|.

    Parameters
    ----------
    v: number
        `v`.

    idx: number
        `idx`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( side, howmny, SELECT, strideSELECT, offsetSELECT, N, T, strideT1, strideT2, offsetT, VL, strideVL1, strideVL2, offsetVL, VR, strideVR1, strideVR2, offsetVR, mm, M, WORK, strideWORK, offsetWORK, lwork, RWORK, strideRWORK, offsetRWORK, lrwork )
    CABS1: |re(z)| + |im(z)| using alternative indexing semantics.

    Parameters
    ----------
    side: string
        specifies the side of the operation.

    howmny: string
        `howmny`.

    SELECT: Int32Array
        `SELECT`.

    strideSELECT: integer
        stride length for `SELECT`.

    offsetSELECT: integer
        starting index for `SELECT`.

    N: integer
        number of columns.

    T: Float64Array
        `T`.

    strideT1: integer
        stride of dimension 1 of `T`.

    strideT2: integer
        stride of dimension 2 of `T`.

    offsetT: integer
        starting index for `T`.

    VL: Float64Array
        `VL`.

    strideVL1: integer
        stride of dimension 1 of `VL`.

    strideVL2: integer
        stride of dimension 2 of `VL`.

    offsetVL: integer
        starting index for `VL`.

    VR: Float64Array
        `VR`.

    strideVR1: integer
        stride of dimension 1 of `VR`.

    strideVR2: integer
        stride of dimension 2 of `VR`.

    offsetVR: integer
        starting index for `VR`.

    mm: integer
        `mm`.

    M: integer
        number of rows.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    lwork: integer
        `lwork`.

    RWORK: Float64Array
        `RWORK`.

    strideRWORK: integer
        stride length for `RWORK`.

    offsetRWORK: integer
        starting index for `RWORK`.

    lrwork: number
        `lrwork`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
