
{{alias}}( N, iw, gl, gu, d, strideD, E2, strideE2, pivmin, reltol, w, werr )
    Computes one eigenvalue of a symmetric tridiagonal matrix to suitable accuracy

    Parameters
    ----------
        N: integer
            number of columns.

        iw: integer
            iw.

        gl: number
            gl.

        gu: number
            gu.

        d: Float64Array
            input array.

        strideD: integer
            stride length for `d`.

        E2: Float64Array
            output array.

        strideE2: integer
            stride length for `E2`.

        pivmin: number
            pivmin.

        reltol: number
            reltol.

        w: number
            w.

        werr: number
            werr.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, iw, gl, gu, d, strideD, offsetD, E2, strideE2, offsetE2, pivmin, reltol, w, werr )
    Computes one eigenvalue of a symmetric tridiagonal matrix to suitable accuracy, using alternative indexing semantics.

    Parameters
    ----------
        N: integer
            number of columns.

        iw: integer
            iw.

        gl: number
            gl.

        gu: number
            gu.

        d: Float64Array
            input array.

        strideD: integer
            stride length for `d`.

        offsetD: integer
            starting index for `D`.

        E2: Float64Array
            output array.

        strideE2: integer
            stride of dimension 2 of `E`.

        offsetE2: integer
            starting index for `E2`.

        pivmin: number
            pivmin.

        reltol: number
            reltol.

        w: number
            w.

        werr: number
            werr.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
