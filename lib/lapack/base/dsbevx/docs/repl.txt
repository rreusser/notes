{{alias}}( jobz, range, uplo, N, kd, AB, LDAB, Q, LDQ, vl, vu, il, iu, abstol, out, w, strideW, Z, LDZ, WORK, strideWORK, IWORK, strideIWORK, IFAIL, strideIFAIL )
    Computes selected eigenvalues and, optionally, eigenvectors of a real
    symmetric band matrix A.

    Parameters
    ----------
    jobz: string
        `jobz`.

    range: string
        `range`.

    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    N: integer
        number of columns.

    kd: number
        `kd`.

    AB: Float64Array
        `AB`.

    LDAB: integer
        leading dimension of `AB`.

    Q: Float64Array
        `Q`.

    LDQ: integer
        leading dimension of `Q`.

    vl: number
        `vl`.

    vu: number
        `vu`.

    il: integer
        `il`.

    iu: integer
        `iu`.

    abstol: number
        `abstol`.

    out: number
        `out`.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    Z: Float64Array
        `Z`.

    LDZ: integer
        leading dimension of `Z`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    IFAIL: Float64Array
        `IFAIL`.

    strideIFAIL: integer
        stride length for `IFAIL`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( jobz, range, uplo, N, kd, AB, strideAB1, strideAB2, offsetAB, Q, strideQ1, strideQ2, offsetQ, vl, vu, il, iu, abstol, out, w, strideW, offsetW, Z, strideZ1, strideZ2, offsetZ, WORK, strideWORK, offsetWORK, IWORK, strideIWORK, offsetIWORK, IFAIL, strideIFAIL, offsetIFAIL )
    Computes selected eigenvalues and, optionally, eigenvectors of a real
    symmetric band matrix A using alternative indexing semantics.

    Parameters
    ----------
    jobz: string
        `jobz`.

    range: string
        `range`.

    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    N: integer
        number of columns.

    kd: number
        `kd`.

    AB: Float64Array
        `AB`.

    strideAB1: integer
        stride of dimension 1 of `AB`.

    strideAB2: integer
        stride of dimension 2 of `AB`.

    offsetAB: integer
        starting index for `AB`.

    Q: Float64Array
        `Q`.

    strideQ1: integer
        stride of dimension 1 of `Q`.

    strideQ2: integer
        stride of dimension 2 of `Q`.

    offsetQ: integer
        starting index for `Q`.

    vl: number
        `vl`.

    vu: number
        `vu`.

    il: integer
        `il`.

    iu: integer
        `iu`.

    abstol: number
        `abstol`.

    out: number
        `out`.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    offsetW: integer
        starting index for `W`.

    Z: Float64Array
        `Z`.

    strideZ1: integer
        stride of dimension 1 of `Z`.

    strideZ2: integer
        stride of dimension 2 of `Z`.

    offsetZ: integer
        starting index for `Z`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    offsetIWORK: integer
        starting index for `IWORK`.

    IFAIL: Float64Array
        `IFAIL`.

    strideIFAIL: integer
        stride length for `IFAIL`.

    offsetIFAIL: integer
        starting index for `IFAIL`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
