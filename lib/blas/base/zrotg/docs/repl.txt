{{alias}}( re, im )
    Computes the parameters for a Givens rotation.

    Parameters
    ----------
    re: number
        `re`.

    im: number
        `im`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( a, offsetA, b, offsetB, c, offsetC, s, offsetS )
    Computes the parameters for a Givens rotation using alternative indexing
    semantics.

    Parameters
    ----------
    a: number
        `a`.

    offsetA: integer
        starting index for `A`.

    b: number
        `b`.

    offsetB: integer
        starting index for `B`.

    c: number
        `c`.

    offsetC: integer
        starting index for `C`.

    s: number
        `s`.

    offsetS: integer
        starting index for `S`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
