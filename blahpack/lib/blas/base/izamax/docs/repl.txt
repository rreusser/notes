{{alias}}( N, zx, strideX )
    Finds the index of the element having the maximum sum of absolute values
    of.

    Parameters
    ----------
    N: integer
        number of columns.

    zx: number
        `zx`.

    strideX: integer
        stride length for `X`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, zx, strideX, offsetX )
    Finds the index of the element having the maximum sum of absolute values
    of using alternative indexing semantics.

    Parameters
    ----------
    N: integer
        number of columns.

    zx: number
        `zx`.

    strideX: integer
        stride length for `X`.

    offsetX: integer
        starting index for `X`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
