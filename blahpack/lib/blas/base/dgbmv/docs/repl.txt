{{alias}}( order, trans, M, N, kl, ku, alpha, A, LDA, x, strideX, beta, y, strideY )
    Performs the matrix-vector operation `y := alpha*op(A)*x + beta*y`.

    Parameters
    ----------
    order: string
        Row-major (C-style) or column-major (Fortran-style) order.

    trans: string
        specifies whether the matrix should be transposed.

    M: integer
        number of rows.

    N: integer
        number of columns.

    kl: integer
        `kl`.

    ku: integer
        `ku`.

    alpha: number
        scalar constant.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    x: Float64Array
        `x`.

    strideX: integer
        stride length for `X`.

    beta: number
        scalar constant.

    y: Float64Array
        `y`.

    strideY: integer
        stride length for `Y`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( trans, M, N, kl, ku, alpha, A, strideA1, strideA2, offsetA, x, strideX, offsetX, beta, y, strideY, offsetY )
    Performs the matrix-vector operation `y := alpha*op(A)*x + beta*y` using
    alternative indexing semantics.

    Parameters
    ----------
    trans: string
        specifies whether the matrix should be transposed.

    M: integer
        number of rows.

    N: integer
        number of columns.

    kl: integer
        `kl`.

    ku: integer
        `ku`.

    alpha: number
        scalar constant.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    x: Float64Array
        `x`.

    strideX: integer
        stride length for `X`.

    offsetX: integer
        starting index for `X`.

    beta: number
        scalar constant.

    y: Float64Array
        `y`.

    strideY: integer
        stride length for `Y`.

    offsetY: integer
        starting index for `Y`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
