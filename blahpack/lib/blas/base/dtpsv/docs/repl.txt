{{alias}}( uplo, trans, diag, N, AP, strideAP, x, strideX )
    Solves one of the systems of equations `A*x = b` or `A^T*x = b`.

    Parameters
    ----------
    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    trans: string
        specifies whether the matrix should be transposed.

    diag: string
        specifies whether the matrix is unit triangular.

    N: integer
        number of columns.

    AP: Float64Array
        `AP`.

    strideAP: integer
        stride length for `AP`.

    x: Float64Array
        `x`.

    strideX: integer
        stride length for `X`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( uplo, trans, diag, N, AP, strideAP, offsetAP, x, strideX, offsetX )
    Solves one of the systems of equations `A*x = b` or `A^T*x = b` using
    alternative indexing semantics.

    Parameters
    ----------
    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    trans: string
        specifies whether the matrix should be transposed.

    diag: string
        specifies whether the matrix is unit triangular.

    N: integer
        number of columns.

    AP: Float64Array
        `AP`.

    strideAP: integer
        stride length for `AP`.

    offsetAP: integer
        starting index for `AP`.

    x: Float64Array
        `x`.

    strideX: integer
        stride length for `X`.

    offsetX: integer
        starting index for `X`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
