{{alias}}( N, zx, strideX, zy, strideY, c, s )
    Applies a real plane rotation to a pair of complex double-precision
    vectors:.

    Parameters
    ----------
    N: integer
        number of columns.

    zx: number
        `zx`.

    strideX: integer
        stride length for `X`.

    zy: number
        `zy`.

    strideY: integer
        stride length for `Y`.

    c: number
        `c`.

    s: number
        `s`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, zx, strideX, offsetX, zy, strideY, offsetY, c, s )
    Applies a real plane rotation to a pair of complex double-precision
    vectors: using alternative indexing semantics.

    Parameters
    ----------
    N: integer
        number of columns.

    zx: number
        `zx`.

    strideX: integer
        stride length for `X`.

    offsetX: integer
        starting index for `X`.

    zy: number
        `zy`.

    strideY: integer
        stride length for `Y`.

    offsetY: integer
        starting index for `Y`.

    c: number
        `c`.

    s: number
        `s`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
