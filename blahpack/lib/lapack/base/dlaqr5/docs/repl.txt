{{alias}}( A, sA1, sA2, oA, i, j )
    Accesses a 2D array element (1-based row i, column j).

    Parameters
    ----------
    A: Float64Array
        `A`.

    sA1: number
        `sA1`.

    sA2: number
        `sA2`.

    oA: number
        `oA`.

    i: number
        `i`.

    j: number
        `j`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( wantt, wantz, kacc22, N, ktop, kbot, nshfts, SR, strideSR, offsetSR, SI, strideSI, offsetSI, H, strideH1, strideH2, offsetH, iloz, ihiz, Z, strideZ1, strideZ2, offsetZ, V, strideV1, strideV2, offsetV, U, strideU1, strideU2, offsetU, nv, WV, strideWV1, strideWV2, offsetWV, nh, WH, strideWH1, strideWH2, offsetWH )
    Accesses a 2D array element (1-based row i, column j) using alternative
    indexing semantics.

    Parameters
    ----------
    wantt: boolean
        `wantt`.

    wantz: boolean
        `wantz`.

    kacc22: number
        `kacc22`.

    N: integer
        number of columns.

    ktop: number
        `ktop`.

    kbot: number
        `kbot`.

    nshfts: integer
        `nshfts`.

    SR: Float64Array
        `SR`.

    strideSR: integer
        stride length for `SR`.

    offsetSR: integer
        starting index for `SR`.

    SI: Float64Array
        `SI`.

    strideSI: integer
        stride length for `SI`.

    offsetSI: integer
        starting index for `SI`.

    H: Float64Array
        `H`.

    strideH1: integer
        stride of dimension 1 of `H`.

    strideH2: integer
        stride of dimension 2 of `H`.

    offsetH: integer
        starting index for `H`.

    iloz: number
        `iloz`.

    ihiz: number
        `ihiz`.

    Z: Float64Array
        `Z`.

    strideZ1: integer
        stride of dimension 1 of `Z`.

    strideZ2: integer
        stride of dimension 2 of `Z`.

    offsetZ: integer
        starting index for `Z`.

    V: Float64Array
        `V`.

    strideV1: integer
        stride of dimension 1 of `V`.

    strideV2: integer
        stride of dimension 2 of `V`.

    offsetV: integer
        starting index for `V`.

    U: Float64Array
        `U`.

    strideU1: integer
        stride of dimension 1 of `U`.

    strideU2: integer
        stride of dimension 2 of `U`.

    offsetU: integer
        starting index for `U`.

    nv: integer
        `nv`.

    WV: Float64Array
        `WV`.

    strideWV1: integer
        stride of dimension 1 of `WV`.

    strideWV2: integer
        stride of dimension 2 of `WV`.

    offsetWV: integer
        starting index for `WV`.

    nh: integer
        `nh`.

    WH: Float64Array
        `WH`.

    strideWH1: integer
        stride of dimension 1 of `WH`.

    strideWH2: integer
        stride of dimension 2 of `WH`.

    offsetWH: integer
        starting index for `WH`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
