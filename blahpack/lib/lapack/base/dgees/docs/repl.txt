{{alias}}( jobvs, sort, select, N, A, LDA, sdim, WR, strideWR, WI, strideWI, VS, LDVS, WORK, strideWORK, lwork, BWORK, strideBWORK )
    Computes for an N-by-N real nonsymmetric matrix A, the eigenvalues, the
    real.

    Parameters
    ----------
    jobvs: string
        `jobvs`.

    sort: string
        `sort`.

    select: number
        `select`.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    sdim: number
        `sdim`.

    WR: Float64Array
        `WR`.

    strideWR: integer
        stride length for `WR`.

    WI: Float64Array
        `WI`.

    strideWI: integer
        stride length for `WI`.

    VS: Float64Array
        `VS`.

    LDVS: integer
        leading dimension of `VS`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    lwork: integer
        `lwork`.

    BWORK: Int32Array
        `BWORK`.

    strideBWORK: integer
        stride length for `BWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( jobvs, sort, select, N, A, strideA1, strideA2, offsetA, sdim, WR, strideWR, offsetWR, WI, strideWI, offsetWI, VS, strideVS1, strideVS2, offsetVS, WORK, strideWORK, offsetWORK, lwork, BWORK, strideBWORK, offsetBWORK )
    Computes for an N-by-N real nonsymmetric matrix A, the eigenvalues, the
    real using alternative indexing semantics.

    Parameters
    ----------
    jobvs: string
        `jobvs`.

    sort: string
        `sort`.

    select: number
        `select`.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    sdim: number
        `sdim`.

    WR: Float64Array
        `WR`.

    strideWR: integer
        stride length for `WR`.

    offsetWR: integer
        starting index for `WR`.

    WI: Float64Array
        `WI`.

    strideWI: integer
        stride length for `WI`.

    offsetWI: integer
        starting index for `WI`.

    VS: Float64Array
        `VS`.

    strideVS1: integer
        stride of dimension 1 of `VS`.

    strideVS2: integer
        stride of dimension 2 of `VS`.

    offsetVS: integer
        starting index for `VS`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    lwork: integer
        `lwork`.

    BWORK: Int32Array
        `BWORK`.

    strideBWORK: integer
        stride length for `BWORK`.

    offsetBWORK: integer
        starting index for `BWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
