{{alias}}( i, D, Z, DELTA, rho, dlam )
    Solves the 2-by-2 secular equation.

    Parameters
    ----------
    i: number
        `i`.

    D: Float64Array
        `D`.

    Z: Float64Array
        `Z`.

    DELTA: Float64Array
        `DELTA`.

    rho: number
        `rho`.

    dlam: number
        `dlam`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( i, D, strideD, offsetD, Z, strideZ, offsetZ, DELTA, strideDELTA, offsetDELTA, rho, dlam )
    Solves the 2-by-2 secular equation using alternative indexing semantics.

    Parameters
    ----------
    i: number
        `i`.

    D: Float64Array
        `D`.

    strideD: integer
        stride length for `D`.

    offsetD: integer
        starting index for `D`.

    Z: Float64Array
        `Z`.

    strideZ: integer
        stride length for `Z`.

    offsetZ: integer
        starting index for `Z`.

    DELTA: Float64Array
        `DELTA`.

    strideDELTA: integer
        stride length for `DELTA`.

    offsetDELTA: integer
        starting index for `DELTA`.

    rho: number
        `rho`.

    dlam: number
        `dlam`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
