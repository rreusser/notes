{{alias}}( N, d, strideD, e, strideE, anorm, rcond, RWORK, strideRWORK )
    Computes the reciprocal of the condition number (in the 1-norm) of a
    complex.

    Parameters
    ----------
    N: integer
        number of columns.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    e: Float64Array
        `e`.

    strideE: integer
        stride length for `E`.

    anorm: number
        `anorm`.

    rcond: number
        `rcond`.

    RWORK: Float64Array
        `RWORK`.

    strideRWORK: integer
        stride length for `RWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, d, strideD, offsetD, e, strideE, offsetE, anorm, rcond, RWORK, strideRWORK, offsetRWORK )
    Computes the reciprocal of the condition number (in the 1-norm) of a
    complex using alternative indexing semantics.

    Parameters
    ----------
    N: integer
        number of columns.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    offsetD: integer
        starting index for `D`.

    e: Float64Array
        `e`.

    strideE: integer
        stride length for `E`.

    offsetE: integer
        starting index for `E`.

    anorm: number
        `anorm`.

    rcond: number
        `rcond`.

    RWORK: Float64Array
        `RWORK`.

    strideRWORK: integer
        stride length for `RWORK`.

    offsetRWORK: integer
        starting index for `RWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
