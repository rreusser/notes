{{alias}}( order, N, kl, ku, ncols, AB, LDAB, AFB, LDAFB )
    Computes the reciprocal pivot growth factor `norm(A)/norm(U)` for a
    complex general banded matrix.

    Parameters
    ----------
    order: string
        Row-major (C-style) or column-major (Fortran-style) order.

    N: integer
        number of columns.

    kl: integer
        `kl`.

    ku: integer
        `ku`.

    ncols: number
        `ncols`.

    AB: Float64Array
        `AB`.

    LDAB: integer
        leading dimension of `AB`.

    AFB: Float64Array
        `AFB`.

    LDAFB: integer
        leading dimension of `AFB`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, kl, ku, ncols, AB, strideAB1, strideAB2, offsetAB, AFB, strideAFB1, strideAFB2, offsetAFB )
    Computes the reciprocal pivot growth factor `norm(A)/norm(U)` for a
    complex general banded matrix using alternative indexing semantics.

    Parameters
    ----------
    N: integer
        number of columns.

    kl: integer
        `kl`.

    ku: integer
        `ku`.

    ncols: number
        `ncols`.

    AB: Float64Array
        `AB`.

    strideAB1: integer
        stride of dimension 1 of `AB`.

    strideAB2: integer
        stride of dimension 2 of `AB`.

    offsetAB: integer
        starting index for `AB`.

    AFB: Float64Array
        `AFB`.

    strideAFB1: integer
        stride of dimension 1 of `AFB`.

    strideAFB2: integer
        stride of dimension 2 of `AFB`.

    offsetAFB: integer
        starting index for `AFB`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
