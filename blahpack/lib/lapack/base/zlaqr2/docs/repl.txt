{{alias}}( v, idx )
    Complex aggressive early deflation (non-recursive).

    Parameters
    ----------
    v: Float64Array
        `v`.

    idx: number
        `idx`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( wantt, wantz, N, ktop, kbot, nw, H, strideH1, strideH2, offsetH, iloz, ihiz, Z, strideZ1, strideZ2, offsetZ, ns, nd, SH, strideSH, offsetSH, V, strideV1, strideV2, offsetV, nhp, T, strideT1, strideT2, offsetT, nvp, WV, strideWV1, strideWV2, offsetWV, WORK, strideWORK, offsetWORK, lwork )
    Complex aggressive early deflation (non-recursive) using alternative
    indexing semantics.

    Parameters
    ----------
    wantt: boolean
        `wantt`.

    wantz: boolean
        `wantz`.

    N: integer
        number of columns.

    ktop: number
        `ktop`.

    kbot: number
        `kbot`.

    nw: integer
        `nw`.

    H: Float64Array
        `H`.

    strideH1: integer
        stride of dimension 1 of `H`.

    strideH2: integer
        stride of dimension 2 of `H`.

    offsetH: integer
        starting index for `H`.

    iloz: number
        `iloz`.

    ihiz: number
        `ihiz`.

    Z: Float64Array
        `Z`.

    strideZ1: integer
        stride of dimension 1 of `Z`.

    strideZ2: integer
        stride of dimension 2 of `Z`.

    offsetZ: integer
        starting index for `Z`.

    ns: number
        `ns`.

    nd: number
        `nd`.

    SH: Float64Array
        `SH`.

    strideSH: integer
        stride length for `SH`.

    offsetSH: integer
        starting index for `SH`.

    V: Float64Array
        `V`.

    strideV1: integer
        stride of dimension 1 of `V`.

    strideV2: integer
        stride of dimension 2 of `V`.

    offsetV: integer
        starting index for `V`.

    nhp: number
        `nhp`.

    T: Float64Array
        `T`.

    strideT1: integer
        stride of dimension 1 of `T`.

    strideT2: integer
        stride of dimension 2 of `T`.

    offsetT: integer
        starting index for `T`.

    nvp: number
        `nvp`.

    WV: Float64Array
        `WV`.

    strideWV1: integer
        stride of dimension 1 of `WV`.

    strideWV2: integer
        stride of dimension 2 of `WV`.

    offsetWV: integer
        starting index for `WV`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    lwork: integer
        `lwork`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
