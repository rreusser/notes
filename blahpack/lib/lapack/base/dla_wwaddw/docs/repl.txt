
{{alias}}( N, x, strideX, y, strideY, w, strideW )
    Adds a vector in doubled-single precision representation

    Parameters
    ----------
        N: integer
            number of columns.

        x: Float64Array
            input array.

        strideX: integer
            stride length for `x`.

        y: Float64Array
            input array.

        strideY: integer
            stride length for `y`.

        w: Float64Array
            output array.

        strideW: integer
            stride length for `w`.

    Returns
    -------
    out: None
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, x, strideX, offsetX, y, strideY, offsetY, w, strideW, offsetW )
    Adds a vector in doubled-single precision representation, using alternative indexing semantics.

    Parameters
    ----------
        N: integer
            number of columns.

        x: Float64Array
            input array.

        strideX: integer
            stride length for `x`.

        offsetX: integer
            starting index for `X`.

        y: Float64Array
            input array.

        strideY: integer
            stride length for `y`.

        offsetY: integer
            starting index for `Y`.

        w: Float64Array
            output array.

        strideW: integer
            stride length for `w`.

        offsetW: integer
            starting index for `W`.

    Returns
    -------
    out: None
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
