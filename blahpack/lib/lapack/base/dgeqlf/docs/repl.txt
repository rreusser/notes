
{{alias}}( order, M, N, A, LDA, TAU, strideTAU, WORK, strideWORK, lwork )
    Computes a QL factorization of a real general matrix

    Parameters
    ----------
        order: string
            Row-major (C-style) or column-major (Fortran-style) order.

        M: integer
            number of rows.

        N: integer
            number of columns.

        A: Float64Array
            input matrix.

        LDA: integer
            leading dimension of `A`.

        TAU: Float64Array
            input array.

        strideTAU: integer
            stride length for `TAU`.

        WORK: Float64Array
            output array.

        strideWORK: integer
            stride length for `WORK`.

        lwork: integer
            lwork.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( M, N, A, strideA1, strideA2, offsetA, TAU, strideTAU, offsetTAU, WORK, strideWORK, offsetWORK, lwork )
    Computes a QL factorization of a real general matrix, using alternative indexing semantics.

    Parameters
    ----------
        M: integer
            number of rows.

        N: integer
            number of columns.

        A: Float64Array
            input matrix.

        strideA1: integer
            stride of dimension 1 of `A`.

        strideA2: integer
            stride of dimension 2 of `A`.

        offsetA: integer
            starting index for `A`.

        TAU: Float64Array
            input array.

        strideTAU: integer
            stride length for `TAU`.

        offsetTAU: integer
            starting index for `TAU`.

        WORK: Float64Array
            output array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: integer
            starting index for `WORK`.

        lwork: integer
            lwork.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
