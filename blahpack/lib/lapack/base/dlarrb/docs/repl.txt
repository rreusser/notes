
{{alias}}( N, d, strideD, LLD, strideLLD, ifirst, ilast, rtol1, rtol2, offset, w, strideW, WGAP, strideWGAP, WERR, strideWERR, WORK, strideWORK, IWORK, strideIWORK, offsetIWORK, pivmin, spdiam, twist )
    Provides limited bisection to locate eigenvalues for more accuracy

    Parameters
    ----------
        N: integer
            number of columns.

        d: Float64Array
            input array.

        strideD: integer
            stride length for `d`.

        LLD: Float64Array
            input array.

        strideLLD: integer
            stride length for `LLD`.

        ifirst: integer
            ifirst.

        ilast: integer
            ilast.

        rtol1: number
            rtol1.

        rtol2: number
            rtol2.

        offset: integer
            offset.

        w: Float64Array
            input array.

        strideW: integer
            stride length for `w`.

        WGAP: Float64Array
            input array.

        strideWGAP: integer
            stride length for `WGAP`.

        WERR: Float64Array
            input array.

        strideWERR: integer
            stride length for `WERR`.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        IWORK: Int32Array
            output array.

        strideIWORK: integer
            stride length for `IWORK`.

        offsetIWORK: integer
            starting index for `IWORK`.

        pivmin: number
            pivmin.

        spdiam: number
            spdiam.

        twist: integer
            twist.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, d, strideD, offsetD, LLD, strideLLD, offsetLLD, ifirst, ilast, rtol1, rtol2, offset, w, strideW, offsetW, WGAP, strideWGAP, offsetWGAP, WERR, strideWERR, offsetWERR, WORK, strideWORK, offsetWORK, IWORK, strideIWORK, offsetIWORK, pivmin, spdiam, twist )
    Provides limited bisection to locate eigenvalues for more accuracy, using alternative indexing semantics.

    Parameters
    ----------
        N: integer
            number of columns.

        d: Float64Array
            input array.

        strideD: integer
            stride length for `d`.

        offsetD: integer
            starting index for `D`.

        LLD: Float64Array
            input array.

        strideLLD: integer
            stride length for `LLD`.

        offsetLLD: integer
            starting index for `LLD`.

        ifirst: integer
            ifirst.

        ilast: integer
            ilast.

        rtol1: number
            rtol1.

        rtol2: number
            rtol2.

        offset: integer
            starting index for ``.

        w: Float64Array
            input array.

        strideW: integer
            stride length for `w`.

        offsetW: integer
            starting index for `W`.

        WGAP: Float64Array
            input array.

        strideWGAP: integer
            stride length for `WGAP`.

        offsetWGAP: integer
            starting index for `WGAP`.

        WERR: Float64Array
            input array.

        strideWERR: integer
            stride length for `WERR`.

        offsetWERR: integer
            starting index for `WERR`.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: integer
            starting index for `WORK`.

        IWORK: Int32Array
            output array.

        strideIWORK: integer
            stride length for `IWORK`.

        offsetIWORK: integer
            starting index for `IWORK`.

        pivmin: number
            pivmin.

        spdiam: number
            spdiam.

        twist: integer
            twist.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
