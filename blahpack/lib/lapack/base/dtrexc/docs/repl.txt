{{alias}}( compq, N, T, LDT, Q, LDQ, ifst, ilst, WORK, strideWORK )
    Reorders the real Schur factorization of a real matrix A = Q_T_Q^T, so
    that.

    Parameters
    ----------
    compq: string
        `compq`.

    N: integer
        number of columns.

    T: Float64Array
        `T`.

    LDT: integer
        leading dimension of `T`.

    Q: Float64Array
        `Q`.

    LDQ: integer
        leading dimension of `Q`.

    ifst: number
        `ifst`.

    ilst: number
        `ilst`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( compq, N, T, strideT1, strideT2, offsetT, Q, strideQ1, strideQ2, offsetQ, ifst, ilst, WORK, strideWORK, offsetWORK )
    Reorders the real Schur factorization of a real matrix A = Q_T_Q^T, so
    that using alternative indexing semantics.

    Parameters
    ----------
    compq: string
        `compq`.

    N: integer
        number of columns.

    T: Float64Array
        `T`.

    strideT1: integer
        stride of dimension 1 of `T`.

    strideT2: integer
        stride of dimension 2 of `T`.

    offsetT: integer
        starting index for `T`.

    Q: Float64Array
        `Q`.

    strideQ1: integer
        stride of dimension 1 of `Q`.

    strideQ2: integer
        stride of dimension 2 of `Q`.

    offsetQ: integer
        starting index for `Q`.

    ifst: number
        `ifst`.

    ilst: number
        `ilst`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
