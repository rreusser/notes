{{alias}}( kniter, orgati, rho, d, z, finit, tau )
    Computes the positive or negative root (closest to the origin) of the
    secular equation.

    Parameters
    ----------
    kniter: number
        `kniter`.

    orgati: number
        `orgati`.

    rho: number
        `rho`.

    d: Float64Array
        `d`.

    z: Float64Array
        `z`.

    finit: number
        `finit`.

    tau: number
        `tau`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( kniter, orgati, rho, d, strideD, offsetD, z, strideZ, offsetZ, finit, tau )
    Computes the positive or negative root (closest to the origin) of the
    secular equation using alternative indexing semantics.

    Parameters
    ----------
    kniter: number
        `kniter`.

    orgati: number
        `orgati`.

    rho: number
        `rho`.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    offsetD: integer
        starting index for `D`.

    z: Float64Array
        `z`.

    strideZ: integer
        stride length for `Z`.

    offsetZ: integer
        starting index for `Z`.

    finit: number
        `finit`.

    tau: number
        `tau`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
