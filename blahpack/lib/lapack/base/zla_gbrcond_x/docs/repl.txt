
{{alias}}( order, trans, N, kl, ku, AB, LDAB, AFB, LDAFB, IPIV, strideIPIV, offsetIPIV, x, strideX, WORK, strideWORK, RWORK, strideRWORK )
    Estimates the infinity norm condition number for a complex general banded matrix with x scaling

    Parameters
    ----------
        order: string
            Row-major (C-style) or column-major (Fortran-style) order.

        trans: string
            specifies the operation type.

        N: integer
            number of columns.

        kl: integer
            kl.

        ku: integer
            ku.

        AB: Float64Array
            input matrix.

        LDAB: integer
            leading dimension of `AB`.

        AFB: Float64Array
            input matrix.

        LDAFB: integer
            leading dimension of `AFB`.

        IPIV: Int32Array
            input array.

        strideIPIV: integer
            stride length for `IPIV`.

        offsetIPIV: integer
            starting index for `IPIV`.

        x: Float64Array
            input array.

        strideX: integer
            stride length for `x`.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        RWORK: Float64Array
            output array.

        strideRWORK: integer
            stride length for `RWORK`.

    Returns
    -------
    out: number
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( trans, N, kl, ku, AB, strideAB1, strideAB2, offsetAB, AFB, strideAFB1, strideAFB2, offsetAFB, IPIV, strideIPIV, offsetIPIV, x, strideX, offsetX, WORK, strideWORK, offsetWORK, RWORK, strideRWORK, offsetRWORK )
    Estimates the infinity norm condition number for a complex general banded matrix with x scaling, using alternative indexing semantics.

    Parameters
    ----------
        trans: string
            specifies the operation type.

        N: integer
            number of columns.

        kl: integer
            kl.

        ku: integer
            ku.

        AB: Float64Array
            input matrix.

        strideAB1: integer
            stride of dimension 1 of `AB`.

        strideAB2: integer
            stride of dimension 2 of `AB`.

        offsetAB: integer
            starting index for `AB`.

        AFB: Float64Array
            input matrix.

        strideAFB1: integer
            stride of dimension 1 of `AFB`.

        strideAFB2: integer
            stride of dimension 2 of `AFB`.

        offsetAFB: integer
            starting index for `AFB`.

        IPIV: Int32Array
            input array.

        strideIPIV: integer
            stride length for `IPIV`.

        offsetIPIV: integer
            starting index for `IPIV`.

        x: Float64Array
            input array.

        strideX: integer
            stride length for `x`.

        offsetX: integer
            starting index for `X`.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: integer
            starting index for `WORK`.

        RWORK: Float64Array
            output array.

        strideRWORK: integer
            stride length for `RWORK`.

        offsetRWORK: integer
            starting index for `RWORK`.

    Returns
    -------
    out: number
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
