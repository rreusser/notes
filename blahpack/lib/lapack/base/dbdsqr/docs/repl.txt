{{alias}}( order, uplo, N, ncvt, nru, ncc, d, strideD, e, strideE, VT, LDVT, U, LDU, C, LDC, WORK, strideWORK )
    Returns |a| with the sign of b (Fortran SIGN intrinsic).

    Parameters
    ----------
    order: string
        Row-major (C-style) or column-major (Fortran-style) order.

    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    N: integer
        number of columns.

    ncvt: integer
        `ncvt`.

    nru: integer
        `nru`.

    ncc: integer
        `ncc`.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    e: Float64Array
        `e`.

    strideE: integer
        stride length for `E`.

    VT: Float64Array
        `VT`.

    LDVT: integer
        leading dimension of `VT`.

    U: Float64Array
        `U`.

    LDU: integer
        leading dimension of `U`.

    C: Float64Array
        `C`.

    LDC: integer
        leading dimension of `C`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( uplo, N, ncvt, nru, ncc, d, strideD, offsetD, e, strideE, offsetE, VT, strideVT1, strideVT2, offsetVT, U, strideU1, strideU2, offsetU, C, strideC1, strideC2, offsetC, WORK, strideWORK, offsetWORK )
    Returns |a| with the sign of b (Fortran SIGN intrinsic) using
    alternative indexing semantics.

    Parameters
    ----------
    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    N: integer
        number of columns.

    ncvt: integer
        `ncvt`.

    nru: integer
        `nru`.

    ncc: integer
        `ncc`.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    offsetD: integer
        starting index for `D`.

    e: Float64Array
        `e`.

    strideE: integer
        stride length for `E`.

    offsetE: integer
        starting index for `E`.

    VT: Float64Array
        `VT`.

    strideVT1: integer
        stride of dimension 1 of `VT`.

    strideVT2: integer
        stride of dimension 2 of `VT`.

    offsetVT: integer
        starting index for `VT`.

    U: Float64Array
        `U`.

    strideU1: integer
        stride of dimension 1 of `U`.

    strideU2: integer
        stride of dimension 2 of `U`.

    offsetU: integer
        starting index for `U`.

    C: Float64Array
        `C`.

    strideC1: integer
        stride of dimension 1 of `C`.

    strideC2: integer
        stride of dimension 2 of `C`.

    offsetC: integer
        starting index for `C`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
