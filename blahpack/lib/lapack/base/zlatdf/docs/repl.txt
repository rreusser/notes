{{alias}}( ijob, N, Z, LDZ, RHS, strideRHS, rdsum, rdscal, IPIV, strideIPIV, JPIV, strideJPIV )
    Computes a contribution to the reciprocal Dif-estimate using the LU
    factorization computed by zgetc2.

    Parameters
    ----------
    ijob: number
        `ijob`.

    N: integer
        number of columns.

    Z: Float64Array
        `Z`.

    LDZ: integer
        leading dimension of `Z`.

    RHS: Float64Array
        `RHS`.

    strideRHS: integer
        stride length for `RHS`.

    rdsum: number
        `rdsum`.

    rdscal: number
        `rdscal`.

    IPIV: Int32Array
        `IPIV`.

    strideIPIV: integer
        stride length for `IPIV`.

    JPIV: Int32Array
        `JPIV`.

    strideJPIV: integer
        stride length for `JPIV`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( ijob, N, Z, strideZ1, strideZ2, offsetZ, RHS, strideRHS, offsetRHS, rdsum, rdscal, IPIV, strideIPIV, offsetIPIV, JPIV, strideJPIV, offsetJPIV )
    Computes a contribution to the reciprocal Dif-estimate using the LU
    factorization computed by zgetc2 using alternative indexing semantics.

    Parameters
    ----------
    ijob: number
        `ijob`.

    N: integer
        number of columns.

    Z: Float64Array
        `Z`.

    strideZ1: integer
        stride of dimension 1 of `Z`.

    strideZ2: integer
        stride of dimension 2 of `Z`.

    offsetZ: integer
        starting index for `Z`.

    RHS: Float64Array
        `RHS`.

    strideRHS: integer
        stride length for `RHS`.

    offsetRHS: integer
        starting index for `RHS`.

    rdsum: number
        `rdsum`.

    rdscal: number
        `rdscal`.

    IPIV: Int32Array
        `IPIV`.

    strideIPIV: integer
        stride length for `IPIV`.

    offsetIPIV: integer
        starting index for `IPIV`.

    JPIV: Int32Array
        `JPIV`.

    strideJPIV: integer
        stride length for `JPIV`.

    offsetJPIV: integer
        starting index for `JPIV`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
