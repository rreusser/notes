{{alias}}( N, kl, ku, nrhs, AB, LDAB, IPIV, strideIPIV, B, LDB )
    Solves a complex system of linear equations `A * X = B` for a banded
    matrix.

    Parameters
    ----------
    N: integer
        number of columns.

    kl: integer
        `kl`.

    ku: integer
        `ku`.

    nrhs: integer
        number of right-hand sides.

    AB: Float64Array
        `AB`.

    LDAB: integer
        leading dimension of `AB`.

    IPIV: Int32Array
        `IPIV`.

    strideIPIV: integer
        stride length for `IPIV`.

    B: Float64Array
        `B`.

    LDB: integer
        leading dimension of `B`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, kl, ku, nrhs, AB, strideAB1, strideAB2, offsetAB, IPIV, strideIPIV, offsetIPIV, B, strideB1, strideB2, offsetB )
    Solves a complex system of linear equations `A * X = B` for a banded
    matrix using alternative indexing semantics.

    Parameters
    ----------
    N: integer
        number of columns.

    kl: integer
        `kl`.

    ku: integer
        `ku`.

    nrhs: integer
        number of right-hand sides.

    AB: Float64Array
        `AB`.

    strideAB1: integer
        stride of dimension 1 of `AB`.

    strideAB2: integer
        stride of dimension 2 of `AB`.

    offsetAB: integer
        starting index for `AB`.

    IPIV: Int32Array
        `IPIV`.

    strideIPIV: integer
        stride length for `IPIV`.

    offsetIPIV: integer
        starting index for `IPIV`.

    B: Float64Array
        `B`.

    strideB1: integer
        stride of dimension 1 of `B`.

    strideB2: integer
        stride of dimension 2 of `B`.

    offsetB: integer
        starting index for `B`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
