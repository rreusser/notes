
{{alias}}( order, uplo, N, A, LDA, IPIV, strideIPIV, offsetIPIV, anorm, rcond, WORK, strideWORK )
    Estimate reciprocal condition number of a complex Hermitian matrix using rook-pivoted factorization

    Parameters
    ----------
        order: string
            Row-major (C-style) or column-major (Fortran-style) order.

        uplo: string
            specifies the operation type.

        N: integer
            number of columns.

        A: Float64Array
            input matrix.

        LDA: integer
            leading dimension of `A`.

        IPIV: Int32Array
            input array.

        strideIPIV: integer
            stride length for `IPIV`.

        offsetIPIV: integer
            starting index for `IPIV`.

        anorm: number
            anorm.

        rcond: number
            rcond.

        WORK: Float64Array
            output array.

        strideWORK: integer
            stride length for `WORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( uplo, N, A, strideA1, strideA2, offsetA, IPIV, strideIPIV, offsetIPIV, anorm, rcond, WORK, strideWORK, offsetWORK )
    Estimate reciprocal condition number of a complex Hermitian matrix using rook-pivoted factorization, using alternative indexing semantics.

    Parameters
    ----------
        uplo: string
            specifies the operation type.

        N: integer
            number of columns.

        A: Float64Array
            input matrix.

        strideA1: integer
            stride of dimension 1 of `A`.

        strideA2: integer
            stride of dimension 2 of `A`.

        offsetA: integer
            starting index for `A`.

        IPIV: Int32Array
            input array.

        strideIPIV: integer
            stride length for `IPIV`.

        offsetIPIV: integer
            starting index for `IPIV`.

        anorm: number
            anorm.

        rcond: number
            rcond.

        WORK: Float64Array
            output array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: integer
            starting index for `WORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
