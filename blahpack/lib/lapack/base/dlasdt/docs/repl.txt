{{alias}}( N, lvl, nd, INODE, strideINODE, NDIML, strideNDIML, NDIMR, strideNDIMR, msub )
    Create a tree of subproblems for bidiagonal divide and conquer.

    Parameters
    ----------
    N: integer
        number of columns.

    lvl: number
        `lvl`.

    nd: number
        `nd`.

    INODE: Float64Array
        `INODE`.

    strideINODE: integer
        stride length for `INODE`.

    NDIML: Float64Array
        `NDIML`.

    strideNDIML: integer
        stride length for `NDIML`.

    NDIMR: Float64Array
        `NDIMR`.

    strideNDIMR: integer
        stride length for `NDIMR`.

    msub: number
        `msub`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, lvl, nd, INODE, strideINODE, offsetINODE, NDIML, strideNDIML, offsetNDIML, NDIMR, strideNDIMR, offsetNDIMR, msub )
    Create a tree of subproblems for bidiagonal divide and conquer using
    alternative indexing semantics.

    Parameters
    ----------
    N: integer
        number of columns.

    lvl: number
        `lvl`.

    nd: number
        `nd`.

    INODE: Float64Array
        `INODE`.

    strideINODE: integer
        stride length for `INODE`.

    offsetINODE: integer
        starting index for `INODE`.

    NDIML: Float64Array
        `NDIML`.

    strideNDIML: integer
        stride length for `NDIML`.

    offsetNDIML: integer
        starting index for `NDIML`.

    NDIMR: Float64Array
        `NDIMR`.

    strideNDIMR: integer
        stride length for `NDIMR`.

    offsetNDIMR: integer
        starting index for `NDIMR`.

    msub: number
        `msub`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
