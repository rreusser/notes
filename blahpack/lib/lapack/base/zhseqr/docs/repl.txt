{{alias}}( job, compz, N, ilo, ihi, H, LDH, w, strideW, Z, LDZ, WORK, strideWORK, lwork )
    Computes the eigenvalues of a complex upper Hessenberg matrix H, and.

    Parameters
    ----------
    job: string
        `job`.

    compz: string
        `compz`.

    N: integer
        number of columns.

    ilo: integer
        `ilo`.

    ihi: integer
        `ihi`.

    H: Float64Array
        `H`.

    LDH: integer
        leading dimension of `H`.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    Z: Float64Array
        `Z`.

    LDZ: integer
        leading dimension of `Z`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    lwork: integer
        `lwork`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( job, compz, N, ilo, ihi, H, strideH1, strideH2, offsetH, w, strideW, offsetW, Z, strideZ1, strideZ2, offsetZ, WORK, strideWORK, offsetWORK, lwork )
    Computes the eigenvalues of a complex upper Hessenberg matrix H, and
    using alternative indexing semantics.

    Parameters
    ----------
    job: string
        `job`.

    compz: string
        `compz`.

    N: integer
        number of columns.

    ilo: integer
        `ilo`.

    ihi: integer
        `ihi`.

    H: Float64Array
        `H`.

    strideH1: integer
        stride of dimension 1 of `H`.

    strideH2: integer
        stride of dimension 2 of `H`.

    offsetH: integer
        starting index for `H`.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    offsetW: integer
        starting index for `W`.

    Z: Float64Array
        `Z`.

    strideZ1: integer
        stride of dimension 1 of `Z`.

    strideZ2: integer
        stride of dimension 2 of `Z`.

    offsetZ: integer
        starting index for `Z`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    lwork: integer
        `lwork`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
