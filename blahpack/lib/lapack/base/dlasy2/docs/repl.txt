{{alias}}( ltranl, ltranr, isgn, n1, n2, TL, LDTL, TR, LDTR, B, LDB, scale, X, LDX, xnorm )
    Solves for the N1-by-N2 matrix X in:.

    Parameters
    ----------
    ltranl: number
        `ltranl`.

    ltranr: number
        `ltranr`.

    isgn: number
        `isgn`.

    n1: number
        `n1`.

    n2: number
        `n2`.

    TL: Float64Array
        `TL`.

    LDTL: integer
        leading dimension of `TL`.

    TR: Float64Array
        `TR`.

    LDTR: integer
        leading dimension of `TR`.

    B: Float64Array
        `B`.

    LDB: integer
        leading dimension of `B`.

    scale: number
        `scale`.

    X: Float64Array
        `X`.

    LDX: integer
        leading dimension of `X`.

    xnorm: number
        `xnorm`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( ltranl, ltranr, isgn, n1, n2, TL, strideTL1, strideTL2, offsetTL, TR, strideTR1, strideTR2, offsetTR, B, strideB1, strideB2, offsetB, scale, X, strideX1, strideX2, offsetX, xnorm )
    Solves for the N1-by-N2 matrix X in: using alternative indexing
    semantics.

    Parameters
    ----------
    ltranl: number
        `ltranl`.

    ltranr: number
        `ltranr`.

    isgn: number
        `isgn`.

    n1: number
        `n1`.

    n2: number
        `n2`.

    TL: Float64Array
        `TL`.

    strideTL1: integer
        stride of dimension 1 of `TL`.

    strideTL2: integer
        stride of dimension 2 of `TL`.

    offsetTL: integer
        starting index for `TL`.

    TR: Float64Array
        `TR`.

    strideTR1: integer
        stride of dimension 1 of `TR`.

    strideTR2: integer
        stride of dimension 2 of `TR`.

    offsetTR: integer
        starting index for `TR`.

    B: Float64Array
        `B`.

    strideB1: integer
        stride of dimension 1 of `B`.

    strideB2: integer
        stride of dimension 2 of `B`.

    offsetB: integer
        starting index for `B`.

    scale: number
        `scale`.

    X: Float64Array
        `X`.

    strideX1: integer
        stride of dimension 1 of `X`.

    strideX2: integer
        stride of dimension 2 of `X`.

    offsetX: integer
        starting index for `X`.

    xnorm: number
        `xnorm`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
