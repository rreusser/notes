
    dstebz.ndarray( range, order, N, vl, vu, il, iu, abstol, d, strideD, offsetD, e, strideE, offsetE, M, nsplit, w, strideW, offsetW, IBLOCK, strideIBLOCK, offsetIBLOCK, ISPLIT, strideISPLIT, offsetISPLIT, WORK, strideWORK, offsetWORK, IWORK, strideIWORK, offsetIWORK )
        Computes selected eigenvalues of a real symmetric tridiagonal matrix by bisection

        Parameters
        ----------
        range: string
            specifies the operation type.

        order: string
            specifies the operation type.

        N: NonNegativeInteger
            number of columns.

        vl: number
            vl.

        vu: number
            vu.

        il: integer
            il.

        iu: integer
            iu.

        abstol: number
            abstol.

        d: Float64Array
            input array.

        strideD: integer
            stride length for `d`.

        offsetD: NonNegativeInteger
            starting index for `d`.

        e: Float64Array
            input array.

        strideE: integer
            stride length for `e`.

        offsetE: NonNegativeInteger
            starting index for `e`.

        M: NonNegativeInteger
            number of rows.

        nsplit: integer
            nsplit.

        w: Float64Array
            input array.

        strideW: integer
            stride length for `w`.

        offsetW: NonNegativeInteger
            starting index for `w`.

        IBLOCK: Int32Array
            input array.

        strideIBLOCK: integer
            stride length for `IBLOCK`.

        offsetIBLOCK: NonNegativeInteger
            starting index for `IBLOCK`.

        ISPLIT: Int32Array
            input array.

        strideISPLIT: integer
            stride length for `ISPLIT`.

        offsetISPLIT: NonNegativeInteger
            starting index for `ISPLIT`.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: NonNegativeInteger
            starting index for `WORK`.

        IWORK: Int32Array
            output array.

        strideIWORK: integer
            stride length for `IWORK`.

        offsetIWORK: NonNegativeInteger
            starting index for `IWORK`.

        Returns
        -------
        out: integer
            TODO: describe return value.

        Examples
        --------
        // TODO: Add REPL examples

    See Also
    --------

