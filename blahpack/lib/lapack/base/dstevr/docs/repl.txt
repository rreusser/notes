{{alias}}( jobz, range, N, d, strideD, e, strideE, vl, vu, il, iu, abstol, out, w, strideW, Z, LDZ, ISUPPZ, strideISUPPZ, WORK, strideWORK, lwork, IWORK, strideIWORK, liwork )
    Computes selected eigenvalues and, optionally, eigenvectors of a real.

    Parameters
    ----------
    jobz: string
        `jobz`.

    range: string
        `range`.

    N: integer
        number of columns.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    e: Float64Array
        `e`.

    strideE: integer
        stride length for `E`.

    vl: number
        `vl`.

    vu: number
        `vu`.

    il: integer
        `il`.

    iu: integer
        `iu`.

    abstol: number
        `abstol`.

    out: number
        `out`.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    Z: Float64Array
        `Z`.

    LDZ: integer
        leading dimension of `Z`.

    ISUPPZ: Int32Array
        `ISUPPZ`.

    strideISUPPZ: integer
        stride length for `ISUPPZ`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    lwork: integer
        `lwork`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    liwork: number
        `liwork`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( jobz, range, N, d, strideD, offsetD, e, strideE, offsetE, vl, vu, il, iu, abstol, out, w, strideW, offsetW, Z, strideZ1, strideZ2, offsetZ, ISUPPZ, strideISUPPZ, offsetISUPPZ, WORK, strideWORK, offsetWORK, lwork, IWORK, strideIWORK, offsetIWORK, liwork )
    Computes selected eigenvalues and, optionally, eigenvectors of a real
    using alternative indexing semantics.

    Parameters
    ----------
    jobz: string
        `jobz`.

    range: string
        `range`.

    N: integer
        number of columns.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    offsetD: integer
        starting index for `D`.

    e: Float64Array
        `e`.

    strideE: integer
        stride length for `E`.

    offsetE: integer
        starting index for `E`.

    vl: number
        `vl`.

    vu: number
        `vu`.

    il: integer
        `il`.

    iu: integer
        `iu`.

    abstol: number
        `abstol`.

    out: number
        `out`.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    offsetW: integer
        starting index for `W`.

    Z: Float64Array
        `Z`.

    strideZ1: integer
        stride of dimension 1 of `Z`.

    strideZ2: integer
        stride of dimension 2 of `Z`.

    offsetZ: integer
        starting index for `Z`.

    ISUPPZ: Int32Array
        `ISUPPZ`.

    strideISUPPZ: integer
        stride length for `ISUPPZ`.

    offsetISUPPZ: integer
        starting index for `ISUPPZ`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    lwork: integer
        `lwork`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    offsetIWORK: integer
        starting index for `IWORK`.

    liwork: number
        `liwork`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
