{{alias}}( N, n1, d, Q, LDQ, INDXQ, rho, z, DLAMBDA, w, Q2, INDX, INDXC, INDXP, COLTYP )
    Merges the two sets of eigenvalues together into a single sorted set,
    then tries to deflate the size of the problem.

    Parameters
    ----------
    N: integer
        number of columns.

    n1: number
        `n1`.

    d: Float64Array
        `d`.

    Q: Float64Array
        `Q`.

    LDQ: integer
        leading dimension of `Q`.

    INDXQ: Float64Array
        `INDXQ`.

    rho: number
        `rho`.

    z: Float64Array
        `z`.

    DLAMBDA: Float64Array
        `DLAMBDA`.

    w: Float64Array
        `w`.

    Q2: Float64Array
        `Q2`.

    INDX: Float64Array
        `INDX`.

    INDXC: Float64Array
        `INDXC`.

    INDXP: Float64Array
        `INDXP`.

    COLTYP: Float64Array
        `COLTYP`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, n1, d, strideD, offsetD, Q, strideQ1, strideQ2, offsetQ, INDXQ, strideINDXQ, offsetINDXQ, rho, z, strideZ, offsetZ, DLAMBDA, strideDLAMBDA, offsetDLAMBDA, w, strideW, offsetW, Q2, strideQ21, offsetQ2, INDX, strideINDX, offsetINDX, INDXC, strideINDXC, offsetINDXC, INDXP, strideINDXP, offsetINDXP, COLTYP, strideCOLTYP, offsetCOLTYP )
    Merges the two sets of eigenvalues together into a single sorted set,
    then tries to deflate the size of the problem using alternative indexing
    semantics.

    Parameters
    ----------
    N: integer
        number of columns.

    n1: number
        `n1`.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    offsetD: integer
        starting index for `D`.

    Q: Float64Array
        `Q`.

    strideQ1: integer
        stride of dimension 1 of `Q`.

    strideQ2: integer
        stride of dimension 2 of `Q`.

    offsetQ: integer
        starting index for `Q`.

    INDXQ: Float64Array
        `INDXQ`.

    strideINDXQ: integer
        stride length for `INDXQ`.

    offsetINDXQ: integer
        starting index for `INDXQ`.

    rho: number
        `rho`.

    z: Float64Array
        `z`.

    strideZ: integer
        stride length for `Z`.

    offsetZ: integer
        starting index for `Z`.

    DLAMBDA: Float64Array
        `DLAMBDA`.

    strideDLAMBDA: integer
        stride length for `DLAMBDA`.

    offsetDLAMBDA: integer
        starting index for `DLAMBDA`.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    offsetW: integer
        starting index for `W`.

    Q2: Float64Array
        `Q2`.

    strideQ21: integer
        stride of dimension 1 of `Q2`.

    offsetQ2: integer
        starting index for `Q2`.

    INDX: Float64Array
        `INDX`.

    strideINDX: integer
        stride length for `INDX`.

    offsetINDX: integer
        starting index for `INDX`.

    INDXC: Float64Array
        `INDXC`.

    strideINDXC: integer
        stride length for `INDXC`.

    offsetINDXC: integer
        starting index for `INDXC`.

    INDXP: Float64Array
        `INDXP`.

    strideINDXP: integer
        stride length for `INDXP`.

    offsetINDXP: integer
        starting index for `INDXP`.

    COLTYP: Float64Array
        `COLTYP`.

    strideCOLTYP: integer
        stride length for `COLTYP`.

    offsetCOLTYP: integer
        starting index for `COLTYP`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
