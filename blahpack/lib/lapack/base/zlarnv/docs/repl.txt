{{alias}}( idist, iseed, strideISEED, N, x, stride )
    Returns a vector of complex random numbers from a uniform or normal
    distribution.

    Parameters
    ----------
    idist: number
        `idist`.

    iseed: Float64Array
        `iseed`.

    strideISEED: integer
        stride length for `ISEED`.

    N: integer
        number of columns.

    x: number
        `x`.

    stride: integer
        `stride`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( idist, iseed, strideISEED, offsetISEED, N, x, stride, offset )
    Returns a vector of complex random numbers from a uniform or normal
    distribution using alternative indexing semantics.

    Parameters
    ----------
    idist: number
        `idist`.

    iseed: Float64Array
        `iseed`.

    strideISEED: integer
        stride length for `ISEED`.

    offsetISEED: integer
        starting index for `ISEED`.

    N: integer
        number of columns.

    x: number
        `x`.

    stride: integer
        `stride`.

    offset: integer
        `offset`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
