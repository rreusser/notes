{{alias}}( jobu, jobv, jobq, M, p, N, A, LDA, B, LDB, tola, tolb, K, l, U, LDU, V, LDV, Q, LDQ, IWORK, strideIWORK, TAU, strideTAU, WORK, strideWORK, lwork )
    Computes orthogonal matrices U, V, and Q such that:.

    Parameters
    ----------
    jobu: string
        `jobu`.

    jobv: number
        `jobv`.

    jobq: number
        `jobq`.

    M: integer
        number of rows.

    p: number
        `p`.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    B: Float64Array
        `B`.

    LDB: integer
        leading dimension of `B`.

    tola: number
        `tola`.

    tolb: number
        `tolb`.

    K: integer
        inner dimension.

    l: number
        `l`.

    U: Float64Array
        `U`.

    LDU: integer
        leading dimension of `U`.

    V: Float64Array
        `V`.

    LDV: integer
        leading dimension of `V`.

    Q: Float64Array
        `Q`.

    LDQ: integer
        leading dimension of `Q`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    TAU: Float64Array
        `TAU`.

    strideTAU: integer
        stride length for `TAU`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    lwork: integer
        `lwork`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( jobu, jobv, jobq, M, p, N, A, strideA1, strideA2, offsetA, B, strideB1, strideB2, offsetB, tola, tolb, K, l, U, strideU1, strideU2, offsetU, V, strideV1, strideV2, offsetV, Q, strideQ1, strideQ2, offsetQ, IWORK, strideIWORK, offsetIWORK, TAU, strideTAU, offsetTAU, WORK, strideWORK, offsetWORK, lwork )
    Computes orthogonal matrices U, V, and Q such that: using alternative
    indexing semantics.

    Parameters
    ----------
    jobu: string
        `jobu`.

    jobv: number
        `jobv`.

    jobq: number
        `jobq`.

    M: integer
        number of rows.

    p: number
        `p`.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    B: Float64Array
        `B`.

    strideB1: integer
        stride of dimension 1 of `B`.

    strideB2: integer
        stride of dimension 2 of `B`.

    offsetB: integer
        starting index for `B`.

    tola: number
        `tola`.

    tolb: number
        `tolb`.

    K: integer
        inner dimension.

    l: number
        `l`.

    U: Float64Array
        `U`.

    strideU1: integer
        stride of dimension 1 of `U`.

    strideU2: integer
        stride of dimension 2 of `U`.

    offsetU: integer
        starting index for `U`.

    V: Float64Array
        `V`.

    strideV1: integer
        stride of dimension 1 of `V`.

    strideV2: integer
        stride of dimension 2 of `V`.

    offsetV: integer
        starting index for `V`.

    Q: Float64Array
        `Q`.

    strideQ1: integer
        stride of dimension 1 of `Q`.

    strideQ2: integer
        stride of dimension 2 of `Q`.

    offsetQ: integer
        starting index for `Q`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    offsetIWORK: integer
        starting index for `IWORK`.

    TAU: Float64Array
        `TAU`.

    strideTAU: integer
        stride length for `TAU`.

    offsetTAU: integer
        starting index for `TAU`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    lwork: integer
        `lwork`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
