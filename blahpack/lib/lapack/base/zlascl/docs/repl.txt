{{alias}}( order, type, kl, ku, cfrom, cto, M, N, A, LDA )
    Multiplies a complex matrix by a real scalar CTO/CFROM, handling
    overflow.

    Parameters
    ----------
    order: string
        Row-major (C-style) or column-major (Fortran-style) order.

    type: number
        `type`.

    kl: integer
        `kl`.

    ku: integer
        `ku`.

    cfrom: number
        `cfrom`.

    cto: number
        `cto`.

    M: integer
        number of rows.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( type, kl, ku, cfrom, cto, M, N, A, strideA1, strideA2, offsetA )
    Multiplies a complex matrix by a real scalar CTO/CFROM, handling
    overflow using alternative indexing semantics.

    Parameters
    ----------
    type: number
        `type`.

    kl: integer
        `kl`.

    ku: integer
        `ku`.

    cfrom: number
        `cfrom`.

    cto: number
        `cto`.

    M: integer
        number of rows.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
