
{{alias}}( order, wantq, wantz, N, A, LDA, B, LDB, Q, LDQ, Z, LDZ, ifst, ilst )
    Reorders the generalized Schur decomposition of a complex matrix pair

    Parameters
    ----------
        order: string
            Row-major (C-style) or column-major (Fortran-style) order.

        wantq: boolean
            wantq.

        wantz: boolean
            wantz.

        N: integer
            number of columns.

        A: Float64Array
            input matrix.

        LDA: integer
            leading dimension of `A`.

        B: Float64Array
            input matrix.

        LDB: integer
            leading dimension of `B`.

        Q: Float64Array
            input matrix.

        LDQ: integer
            leading dimension of `Q`.

        Z: Float64Array
            output matrix.

        LDZ: integer
            leading dimension of `Z`.

        ifst: integer
            ifst.

        ilst: integer
            ilst.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( wantq, wantz, N, A, strideA1, strideA2, offsetA, B, strideB1, strideB2, offsetB, Q, strideQ1, strideQ2, offsetQ, Z, strideZ1, strideZ2, offsetZ, ifst, ilst )
    Reorders the generalized Schur decomposition of a complex matrix pair, using alternative indexing semantics.

    Parameters
    ----------
        wantq: boolean
            wantq.

        wantz: boolean
            wantz.

        N: integer
            number of columns.

        A: Float64Array
            input matrix.

        strideA1: integer
            stride of dimension 1 of `A`.

        strideA2: integer
            stride of dimension 2 of `A`.

        offsetA: integer
            starting index for `A`.

        B: Float64Array
            input matrix.

        strideB1: integer
            stride of dimension 1 of `B`.

        strideB2: integer
            stride of dimension 2 of `B`.

        offsetB: integer
            starting index for `B`.

        Q: Float64Array
            input matrix.

        strideQ1: integer
            stride of dimension 1 of `Q`.

        strideQ2: integer
            stride of dimension 2 of `Q`.

        offsetQ: integer
            starting index for `Q`.

        Z: Float64Array
            output matrix.

        strideZ1: integer
            stride of dimension 1 of `Z`.

        strideZ2: integer
            stride of dimension 2 of `Z`.

        offsetZ: integer
            starting index for `Z`.

        ifst: integer
            ifst.

        ilst: integer
            ilst.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
