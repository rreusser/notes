
{{alias}}( N, d, strideD, LLD, strideLLD, sigma, pivmin, r )
    Computes the Sturm count for a tridiagonal matrix

    Parameters
    ----------
        N: integer
            number of columns.

        d: Float64Array
            input array.

        strideD: integer
            stride length for `d`.

        LLD: Float64Array
            output array.

        strideLLD: integer
            stride length for `LLD`.

        sigma: number
            sigma.

        pivmin: number
            pivmin.

        r: integer
            r.

    Returns
    -------
    out: number
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, d, strideD, offsetD, LLD, strideLLD, offsetLLD, sigma, pivmin, r )
    Computes the Sturm count for a tridiagonal matrix, using alternative indexing semantics.

    Parameters
    ----------
        N: integer
            number of columns.

        d: Float64Array
            input array.

        strideD: integer
            stride length for `d`.

        offsetD: integer
            starting index for `D`.

        LLD: Float64Array
            output array.

        strideLLD: integer
            stride length for `LLD`.

        offsetLLD: integer
            starting index for `LLD`.

        sigma: number
            sigma.

        pivmin: number
            pivmin.

        r: integer
            r.

    Returns
    -------
    out: number
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
