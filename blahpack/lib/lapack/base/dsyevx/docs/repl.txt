{{alias}}( jobz, range, uplo, N, A, LDA, vl, vu, il, iu, abstol, out, w, strideW, Z, LDZ, WORK, strideWORK, lwork, IWORK, strideIWORK, IFAIL, strideIFAIL )
    Computes selected eigenvalues and, optionally, eigenvectors of a real.

    Parameters
    ----------
    jobz: string
        `jobz`.

    range: string
        `range`.

    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    vl: number
        `vl`.

    vu: number
        `vu`.

    il: integer
        `il`.

    iu: integer
        `iu`.

    abstol: number
        `abstol`.

    out: number
        `out`.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    Z: Float64Array
        `Z`.

    LDZ: integer
        leading dimension of `Z`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    lwork: integer
        `lwork`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    IFAIL: Float64Array
        `IFAIL`.

    strideIFAIL: integer
        stride length for `IFAIL`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( jobz, range, uplo, N, A, strideA1, strideA2, offsetA, vl, vu, il, iu, abstol, out, w, strideW, offsetW, Z, strideZ1, strideZ2, offsetZ, WORK, strideWORK, offsetWORK, lwork, IWORK, strideIWORK, offsetIWORK, IFAIL, strideIFAIL, offsetIFAIL )
    Computes selected eigenvalues and, optionally, eigenvectors of a real
    using alternative indexing semantics.

    Parameters
    ----------
    jobz: string
        `jobz`.

    range: string
        `range`.

    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    N: integer
        number of columns.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    vl: number
        `vl`.

    vu: number
        `vu`.

    il: integer
        `il`.

    iu: integer
        `iu`.

    abstol: number
        `abstol`.

    out: number
        `out`.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    offsetW: integer
        starting index for `W`.

    Z: Float64Array
        `Z`.

    strideZ1: integer
        stride of dimension 1 of `Z`.

    strideZ2: integer
        stride of dimension 2 of `Z`.

    offsetZ: integer
        starting index for `Z`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    lwork: integer
        `lwork`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    offsetIWORK: integer
        starting index for `IWORK`.

    IFAIL: Float64Array
        `IFAIL`.

    strideIFAIL: integer
        stride length for `IFAIL`.

    offsetIFAIL: integer
        starting index for `IFAIL`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
