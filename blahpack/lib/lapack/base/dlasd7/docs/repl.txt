{{alias}}( icompq, nl, nr, sqre, d, z, ZW, VF, VFW, VL, VLW, alpha, beta, DSIGMA, IDX, IDXP, IDXQ, PERM, GIVCOL, LDGCOL, GIVNUM, LDGNUM )
    @license Apache-2.0.

    Parameters
    ----------
    icompq: number
        `icompq`.

    nl: number
        `nl`.

    nr: number
        `nr`.

    sqre: number
        `sqre`.

    d: Float64Array
        `d`.

    z: Float64Array
        `z`.

    ZW: Float64Array
        `ZW`.

    VF: Float64Array
        `VF`.

    VFW: Float64Array
        `VFW`.

    VL: Float64Array
        `VL`.

    VLW: Float64Array
        `VLW`.

    alpha: number
        scalar constant.

    beta: number
        scalar constant.

    DSIGMA: Float64Array
        `DSIGMA`.

    IDX: Int32Array
        `IDX`.

    IDXP: Int32Array
        `IDXP`.

    IDXQ: Int32Array
        `IDXQ`.

    PERM: Int32Array
        `PERM`.

    GIVCOL: Int32Array
        `GIVCOL`.

    LDGCOL: integer
        leading dimension of `GCOL`.

    GIVNUM: Float64Array
        `GIVNUM`.

    LDGNUM: integer
        leading dimension of `GNUM`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( icompq, nl, nr, sqre, d, strideD, offsetD, z, strideZ, offsetZ, ZW, strideZW, offsetZW, VF, strideVF, offsetVF, VFW, strideVFW, offsetVFW, VL, strideVL, offsetVL, VLW, strideVLW, offsetVLW, alpha, beta, DSIGMA, strideDSIGMA, offsetDSIGMA, IDX, strideIDX, offsetIDX, IDXP, strideIDXP, offsetIDXP, IDXQ, strideIDXQ, offsetIDXQ, PERM, stridePERM, offsetPERM, GIVCOL, strideGIVCOL1, strideGIVCOL2, offsetGIVCOL, GIVNUM, strideGIVNUM1, strideGIVNUM2, offsetGIVNUM )
    @license Apache-2.0 using alternative indexing semantics.

    Parameters
    ----------
    icompq: number
        `icompq`.

    nl: number
        `nl`.

    nr: number
        `nr`.

    sqre: number
        `sqre`.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    offsetD: integer
        starting index for `D`.

    z: Float64Array
        `z`.

    strideZ: integer
        stride length for `Z`.

    offsetZ: integer
        starting index for `Z`.

    ZW: Float64Array
        `ZW`.

    strideZW: integer
        stride length for `ZW`.

    offsetZW: integer
        starting index for `ZW`.

    VF: Float64Array
        `VF`.

    strideVF: integer
        stride length for `VF`.

    offsetVF: integer
        starting index for `VF`.

    VFW: Float64Array
        `VFW`.

    strideVFW: integer
        stride length for `VFW`.

    offsetVFW: integer
        starting index for `VFW`.

    VL: Float64Array
        `VL`.

    strideVL: integer
        stride length for `VL`.

    offsetVL: integer
        starting index for `VL`.

    VLW: Float64Array
        `VLW`.

    strideVLW: integer
        stride length for `VLW`.

    offsetVLW: integer
        starting index for `VLW`.

    alpha: number
        scalar constant.

    beta: number
        scalar constant.

    DSIGMA: Float64Array
        `DSIGMA`.

    strideDSIGMA: integer
        stride length for `DSIGMA`.

    offsetDSIGMA: integer
        starting index for `DSIGMA`.

    IDX: Int32Array
        `IDX`.

    strideIDX: integer
        stride length for `IDX`.

    offsetIDX: integer
        starting index for `IDX`.

    IDXP: Int32Array
        `IDXP`.

    strideIDXP: integer
        stride length for `IDXP`.

    offsetIDXP: integer
        starting index for `IDXP`.

    IDXQ: Int32Array
        `IDXQ`.

    strideIDXQ: integer
        stride length for `IDXQ`.

    offsetIDXQ: integer
        starting index for `IDXQ`.

    PERM: Int32Array
        `PERM`.

    stridePERM: integer
        stride length for `PERM`.

    offsetPERM: integer
        starting index for `PERM`.

    GIVCOL: Int32Array
        `GIVCOL`.

    strideGIVCOL1: integer
        stride of dimension 1 of `GIVCOL`.

    strideGIVCOL2: integer
        stride of dimension 2 of `GIVCOL`.

    offsetGIVCOL: integer
        starting index for `GIVCOL`.

    GIVNUM: Float64Array
        `GIVNUM`.

    strideGIVNUM1: integer
        stride of dimension 1 of `GIVNUM`.

    strideGIVNUM2: integer
        stride of dimension 2 of `GIVNUM`.

    offsetGIVNUM: integer
        starting index for `GIVNUM`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
