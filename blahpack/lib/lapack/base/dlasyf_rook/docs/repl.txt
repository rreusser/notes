
{{alias}}( order, uplo, N, nb, kb, A, LDA, IPIV, strideIPIV, offsetIPIV, W, LDW )
    Compute a partial factorization of a real symmetric matrix using the bounded Bunch-Kaufman (rook) diagonal pivoting method

    Parameters
    ----------
        order: string
            Row-major (C-style) or column-major (Fortran-style) order.

        uplo: string
            specifies the operation type.

        N: integer
            number of columns.

        nb: integer
            nb.

        kb: integer
            kb.

        A: Float64Array
            input matrix.

        LDA: integer
            leading dimension of `A`.

        IPIV: Int32Array
            input array.

        strideIPIV: integer
            stride length for `IPIV`.

        offsetIPIV: integer
            starting index for `IPIV`.

        W: Float64Array
            output matrix.

        LDW: integer
            leading dimension of `W`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( uplo, N, nb, kb, A, strideA1, strideA2, offsetA, IPIV, strideIPIV, offsetIPIV, W, strideW1, strideW2, offsetW )
    Compute a partial factorization of a real symmetric matrix using the bounded Bunch-Kaufman (rook) diagonal pivoting method, using alternative indexing semantics.

    Parameters
    ----------
        uplo: string
            specifies the operation type.

        N: integer
            number of columns.

        nb: integer
            nb.

        kb: integer
            kb.

        A: Float64Array
            input matrix.

        strideA1: integer
            stride of dimension 1 of `A`.

        strideA2: integer
            stride of dimension 2 of `A`.

        offsetA: integer
            starting index for `A`.

        IPIV: Int32Array
            input array.

        strideIPIV: integer
            stride length for `IPIV`.

        offsetIPIV: integer
            starting index for `IPIV`.

        W: Float64Array
            output matrix.

        strideW1: integer
            stride of dimension 1 of `W`.

        strideW2: integer
            stride of dimension 2 of `W`.

        offsetW: integer
            starting index for `W`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
