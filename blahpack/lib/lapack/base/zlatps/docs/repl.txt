{{alias}}( uplo, trans, diag, normin, N, AP, x, scale, CNORM )
    Solves a complex triangular system with scaling to prevent overflow,
    where the matrix is in packed storage.

    Parameters
    ----------
    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    trans: string
        specifies whether the matrix should be transposed.

    diag: string
        specifies whether the matrix is unit triangular.

    normin: string
        `normin`.

    N: integer
        number of columns.

    AP: Float64Array
        `AP`.

    x: Float64Array
        `x`.

    scale: number
        `scale`.

    CNORM: Float64Array
        `CNORM`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( uplo, trans, diag, normin, N, AP, strideAP, offsetAP, x, strideX, offsetX, scale, CNORM, strideCNORM, offsetCNORM )
    Solves a complex triangular system with scaling to prevent overflow,
    where the matrix is in packed storage using alternative indexing
    semantics.

    Parameters
    ----------
    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    trans: string
        specifies whether the matrix should be transposed.

    diag: string
        specifies whether the matrix is unit triangular.

    normin: string
        `normin`.

    N: integer
        number of columns.

    AP: Float64Array
        `AP`.

    strideAP: integer
        stride length for `AP`.

    offsetAP: integer
        starting index for `AP`.

    x: Float64Array
        `x`.

    strideX: integer
        stride length for `X`.

    offsetX: integer
        starting index for `X`.

    scale: number
        `scale`.

    CNORM: Float64Array
        `CNORM`.

    strideCNORM: integer
        stride length for `CNORM`.

    offsetCNORM: integer
        starting index for `CNORM`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
