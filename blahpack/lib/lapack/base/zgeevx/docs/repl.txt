
{{alias}}( order, balanc, jobvl, jobvr, sense, N, A, LDA, w, strideW, VL, LDVL, VR, LDVR, ilo, ihi, SCALE, strideSCALE, abnrm, RCONDE, strideRCONDE, RCONDV, strideRCONDV, WORK, strideWORK, lwork, RWORK, strideRWORK )
    Computes eigenvalues, eigenvectors, and reciprocal condition numbers for a complex nonsymmetric matrix

    Parameters
    ----------
        order: string
            Row-major (C-style) or column-major (Fortran-style) order.

        balanc: string
            specifies the operation type.

        jobvl: string
            specifies the operation type.

        jobvr: string
            specifies the operation type.

        sense: string
            specifies the operation type.

        N: integer
            number of columns.

        A: Float64Array
            input matrix.

        LDA: integer
            leading dimension of `A`.

        w: Float64Array
            input array.

        strideW: integer
            stride length for `w`.

        VL: Float64Array
            input matrix.

        LDVL: integer
            leading dimension of `VL`.

        VR: Float64Array
            input matrix.

        LDVR: integer
            leading dimension of `VR`.

        ilo: integer
            ilo.

        ihi: integer
            ihi.

        SCALE: Float64Array
            input array.

        strideSCALE: integer
            stride length for `SCALE`.

        abnrm: number
            abnrm.

        RCONDE: Float64Array
            input array.

        strideRCONDE: integer
            stride length for `RCONDE`.

        RCONDV: Float64Array
            input array.

        strideRCONDV: integer
            stride length for `RCONDV`.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        lwork: integer
            lwork.

        RWORK: Float64Array
            output array.

        strideRWORK: integer
            stride length for `RWORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( balanc, jobvl, jobvr, sense, N, A, strideA1, strideA2, offsetA, w, strideW, offsetW, VL, strideVL1, strideVL2, offsetVL, VR, strideVR1, strideVR2, offsetVR, ilo, ihi, SCALE, strideSCALE, offsetSCALE, abnrm, RCONDE, strideRCONDE, offsetRCONDE, RCONDV, strideRCONDV, offsetRCONDV, WORK, strideWORK, offsetWORK, lwork, RWORK, strideRWORK, offsetRWORK )
    Computes eigenvalues, eigenvectors, and reciprocal condition numbers for a complex nonsymmetric matrix, using alternative indexing semantics.

    Parameters
    ----------
        balanc: string
            specifies the operation type.

        jobvl: string
            specifies the operation type.

        jobvr: string
            specifies the operation type.

        sense: string
            specifies the operation type.

        N: integer
            number of columns.

        A: Float64Array
            input matrix.

        strideA1: integer
            stride of dimension 1 of `A`.

        strideA2: integer
            stride of dimension 2 of `A`.

        offsetA: integer
            starting index for `A`.

        w: Float64Array
            input array.

        strideW: integer
            stride length for `w`.

        offsetW: integer
            starting index for `W`.

        VL: Float64Array
            input matrix.

        strideVL1: integer
            stride of dimension 1 of `VL`.

        strideVL2: integer
            stride of dimension 2 of `VL`.

        offsetVL: integer
            starting index for `VL`.

        VR: Float64Array
            input matrix.

        strideVR1: integer
            stride of dimension 1 of `VR`.

        strideVR2: integer
            stride of dimension 2 of `VR`.

        offsetVR: integer
            starting index for `VR`.

        ilo: integer
            ilo.

        ihi: integer
            ihi.

        SCALE: Float64Array
            input array.

        strideSCALE: integer
            stride length for `SCALE`.

        offsetSCALE: integer
            starting index for `SCALE`.

        abnrm: number
            abnrm.

        RCONDE: Float64Array
            input array.

        strideRCONDE: integer
            stride length for `RCONDE`.

        offsetRCONDE: integer
            starting index for `RCONDE`.

        RCONDV: Float64Array
            input array.

        strideRCONDV: integer
            stride length for `RCONDV`.

        offsetRCONDV: integer
            starting index for `RCONDV`.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: integer
            starting index for `WORK`.

        lwork: integer
            lwork.

        RWORK: Float64Array
            output array.

        strideRWORK: integer
            stride length for `RWORK`.

        offsetRWORK: integer
            starting index for `RWORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
