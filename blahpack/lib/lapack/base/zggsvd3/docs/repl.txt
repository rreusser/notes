
{{alias}}( order, jobu, jobv, jobq, M, N, p, K, l, A, LDA, B, LDB, ALPHA, strideALPHA, BETA, strideBETA, U, LDU, V, LDV, Q, LDQ, WORK, strideWORK, lwork, RWORK, strideRWORK, IWORK, strideIWORK, offsetIWORK )
    Computes the generalized SVD of a pair of complex matrices

    Parameters
    ----------
        order: string
            Row-major (C-style) or column-major (Fortran-style) order.

        jobu: string
            specifies the operation type.

        jobv: string
            specifies the operation type.

        jobq: string
            specifies the operation type.

        M: integer
            number of rows.

        N: integer
            number of columns.

        p: integer
            p.

        K: integer
            number of superdiagonals.

        l: integer
            l.

        A: Float64Array
            input matrix.

        LDA: integer
            leading dimension of `A`.

        B: Float64Array
            input matrix.

        LDB: integer
            leading dimension of `B`.

        ALPHA: Float64Array
            input array.

        strideALPHA: integer
            stride length for `ALPHA`.

        BETA: Float64Array
            input array.

        strideBETA: integer
            stride length for `BETA`.

        U: Float64Array
            input matrix.

        LDU: integer
            leading dimension of `U`.

        V: Float64Array
            input matrix.

        LDV: integer
            leading dimension of `V`.

        Q: Float64Array
            input matrix.

        LDQ: integer
            leading dimension of `Q`.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        lwork: integer
            lwork.

        RWORK: Float64Array
            input array.

        strideRWORK: integer
            stride length for `RWORK`.

        IWORK: Int32Array
            output array.

        strideIWORK: integer
            stride length for `IWORK`.

        offsetIWORK: integer
            starting index for `IWORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( jobu, jobv, jobq, M, N, p, K, l, A, strideA1, strideA2, offsetA, B, strideB1, strideB2, offsetB, ALPHA, strideALPHA, offsetALPHA, BETA, strideBETA, offsetBETA, U, strideU1, strideU2, offsetU, V, strideV1, strideV2, offsetV, Q, strideQ1, strideQ2, offsetQ, WORK, strideWORK, offsetWORK, lwork, RWORK, strideRWORK, offsetRWORK, IWORK, strideIWORK, offsetIWORK )
    Computes the generalized SVD of a pair of complex matrices, using alternative indexing semantics.

    Parameters
    ----------
        jobu: string
            specifies the operation type.

        jobv: string
            specifies the operation type.

        jobq: string
            specifies the operation type.

        M: integer
            number of rows.

        N: integer
            number of columns.

        p: integer
            p.

        K: integer
            number of superdiagonals.

        l: integer
            l.

        A: Float64Array
            input matrix.

        strideA1: integer
            stride of dimension 1 of `A`.

        strideA2: integer
            stride of dimension 2 of `A`.

        offsetA: integer
            starting index for `A`.

        B: Float64Array
            input matrix.

        strideB1: integer
            stride of dimension 1 of `B`.

        strideB2: integer
            stride of dimension 2 of `B`.

        offsetB: integer
            starting index for `B`.

        ALPHA: Float64Array
            input array.

        strideALPHA: integer
            stride length for `ALPHA`.

        offsetALPHA: integer
            starting index for `ALPHA`.

        BETA: Float64Array
            input array.

        strideBETA: integer
            stride length for `BETA`.

        offsetBETA: integer
            starting index for `BETA`.

        U: Float64Array
            input matrix.

        strideU1: integer
            stride of dimension 1 of `U`.

        strideU2: integer
            stride of dimension 2 of `U`.

        offsetU: integer
            starting index for `U`.

        V: Float64Array
            input matrix.

        strideV1: integer
            stride of dimension 1 of `V`.

        strideV2: integer
            stride of dimension 2 of `V`.

        offsetV: integer
            starting index for `V`.

        Q: Float64Array
            input matrix.

        strideQ1: integer
            stride of dimension 1 of `Q`.

        strideQ2: integer
            stride of dimension 2 of `Q`.

        offsetQ: integer
            starting index for `Q`.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: integer
            starting index for `WORK`.

        lwork: integer
            lwork.

        RWORK: Float64Array
            input array.

        strideRWORK: integer
            stride length for `RWORK`.

        offsetRWORK: integer
            starting index for `RWORK`.

        IWORK: Int32Array
            output array.

        strideIWORK: integer
            stride length for `IWORK`.

        offsetIWORK: integer
            starting index for `IWORK`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
