
    zhegvx.ndarray( itype, jobz, range, uplo, N, A, strideA1, strideA2, offsetA, B, strideB1, strideB2, offsetB, vl, vu, il, iu, abstol, M, w, strideW, offsetW, Z, strideZ1, strideZ2, offsetZ, WORK, strideWORK, offsetWORK, lwork, RWORK, strideRWORK, offsetRWORK, IWORK, strideIWORK, offsetIWORK, IFAIL, strideIFAIL, offsetIFAIL )
        Computes selected eigenvalues and eigenvectors of a complex generalized Hermitian-definite eigenproblem

        Parameters
        ----------
        itype: integer
            itype.

        jobz: string
            specifies the operation type.

        range: string
            specifies the operation type.

        uplo: string
            specifies the operation type.

        N: NonNegativeInteger
            number of columns.

        A: Float64Array
            input matrix.

        strideA1: integer
            stride of the first dimension of `A`.

        strideA2: integer
            stride of the second dimension of `A`.

        offsetA: NonNegativeInteger
            starting index for `A`.

        B: Float64Array
            input matrix.

        strideB1: integer
            stride of the first dimension of `B`.

        strideB2: integer
            stride of the second dimension of `B`.

        offsetB: NonNegativeInteger
            starting index for `B`.

        vl: number
            vl.

        vu: number
            vu.

        il: integer
            il.

        iu: integer
            iu.

        abstol: number
            abstol.

        M: NonNegativeInteger
            number of rows.

        w: Float64Array
            input array.

        strideW: integer
            stride length for `w`.

        offsetW: NonNegativeInteger
            starting index for `w`.

        Z: Float64Array
            input matrix.

        strideZ1: integer
            stride of the first dimension of `Z`.

        strideZ2: integer
            stride of the second dimension of `Z`.

        offsetZ: NonNegativeInteger
            starting index for `Z`.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: NonNegativeInteger
            starting index for `WORK`.

        lwork: integer
            lwork.

        RWORK: Float64Array
            input array.

        strideRWORK: integer
            stride length for `RWORK`.

        offsetRWORK: NonNegativeInteger
            starting index for `RWORK`.

        IWORK: Int32Array
            input array.

        strideIWORK: integer
            stride length for `IWORK`.

        offsetIWORK: NonNegativeInteger
            starting index for `IWORK`.

        IFAIL: Int32Array
            output array.

        strideIFAIL: integer
            stride length for `IFAIL`.

        offsetIFAIL: NonNegativeInteger
            starting index for `IFAIL`.

        Returns
        -------
        out: integer
            TODO: describe return value.

        Examples
        --------
        // TODO: Add REPL examples

    See Also
    --------

