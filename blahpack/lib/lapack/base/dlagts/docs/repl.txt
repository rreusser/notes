{{alias}}( job, N, a, strideA, b, strideB, c, strideC, d, strideD, IN, strideIN, y, strideY, tol )
    Solves the system (T - lambda_I)_x = y or (T - lambda_I)__T_x = y using.

    Parameters
    ----------
    job: string
        `job`.

    N: integer
        number of columns.

    a: Float64Array
        `a`.

    strideA: integer
        stride length for `A`.

    b: Float64Array
        `b`.

    strideB: integer
        stride length for `B`.

    c: Float64Array
        `c`.

    strideC: integer
        stride length for `C`.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    IN: Float64Array
        `IN`.

    strideIN: integer
        stride length for `IN`.

    y: Float64Array
        `y`.

    strideY: integer
        stride length for `Y`.

    tol: number
        `tol`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( job, N, a, strideA, offsetA, b, strideB, offsetB, c, strideC, offsetC, d, strideD, offsetD, IN, strideIN, offsetIN, y, strideY, offsetY, tol )
    Solves the system (T - lambda_I)_x = y or (T - lambda_I)__T_x = y using
    using alternative indexing semantics.

    Parameters
    ----------
    job: string
        `job`.

    N: integer
        number of columns.

    a: Float64Array
        `a`.

    strideA: integer
        stride length for `A`.

    offsetA: integer
        starting index for `A`.

    b: Float64Array
        `b`.

    strideB: integer
        stride length for `B`.

    offsetB: integer
        starting index for `B`.

    c: Float64Array
        `c`.

    strideC: integer
        stride length for `C`.

    offsetC: integer
        starting index for `C`.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    offsetD: integer
        starting index for `D`.

    IN: Float64Array
        `IN`.

    strideIN: integer
        stride length for `IN`.

    offsetIN: integer
        starting index for `IN`.

    y: Float64Array
        `y`.

    strideY: integer
        stride length for `Y`.

    offsetY: integer
        starting index for `Y`.

    tol: number
        `tol`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
