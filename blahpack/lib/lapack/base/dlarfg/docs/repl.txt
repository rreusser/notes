{{alias}}( N, alpha, offsetAlpha, x, strideX, tau, offsetTau )
    Generates a real elementary reflector H of order N, such that.

    Parameters
    ----------
    N: integer
        number of columns.

    alpha: number
        scalar constant.

    offsetAlpha: integer
        starting index for `Alpha`.

    x: Float64Array
        `x`.

    strideX: integer
        stride length for `X`.

    tau: number
        `tau`.

    offsetTau: integer
        starting index for `Tau`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, alpha, offsetAlpha, x, strideX, offsetX, tau, offsetTau )
    Generates a real elementary reflector H of order N, such that using
    alternative indexing semantics.

    Parameters
    ----------
    N: integer
        number of columns.

    alpha: number
        scalar constant.

    offsetAlpha: integer
        starting index for `Alpha`.

    x: Float64Array
        `x`.

    strideX: integer
        stride length for `X`.

    offsetX: integer
        starting index for `X`.

    tau: number
        `tau`.

    offsetTau: integer
        starting index for `Tau`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
