{{alias}}( uplo, trans, diag, normin, N, kd, AB, LDAB, x, strideX, scale, CNORM, strideCNORM )
    Solves a triangular banded system with scaling to prevent overflow.

    Parameters
    ----------
    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    trans: string
        specifies whether the matrix should be transposed.

    diag: string
        specifies whether the matrix is unit triangular.

    normin: string
        `normin`.

    N: integer
        number of columns.

    kd: number
        `kd`.

    AB: Float64Array
        `AB`.

    LDAB: integer
        leading dimension of `AB`.

    x: Float64Array
        `x`.

    strideX: integer
        stride length for `X`.

    scale: number
        `scale`.

    CNORM: Float64Array
        `CNORM`.

    strideCNORM: integer
        stride length for `CNORM`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( uplo, trans, diag, normin, N, kd, AB, strideAB1, strideAB2, offsetAB, x, strideX, offsetX, scale, CNORM, strideCNORM, offsetCNORM )
    Solves a triangular banded system with scaling to prevent overflow using
    alternative indexing semantics.

    Parameters
    ----------
    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    trans: string
        specifies whether the matrix should be transposed.

    diag: string
        specifies whether the matrix is unit triangular.

    normin: string
        `normin`.

    N: integer
        number of columns.

    kd: number
        `kd`.

    AB: Float64Array
        `AB`.

    strideAB1: integer
        stride of dimension 1 of `AB`.

    strideAB2: integer
        stride of dimension 2 of `AB`.

    offsetAB: integer
        starting index for `AB`.

    x: Float64Array
        `x`.

    strideX: integer
        stride length for `X`.

    offsetX: integer
        starting index for `X`.

    scale: number
        `scale`.

    CNORM: Float64Array
        `CNORM`.

    strideCNORM: integer
        stride length for `CNORM`.

    offsetCNORM: integer
        starting index for `CNORM`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
