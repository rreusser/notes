{{alias}}( order, job, compq, compz, N, ilo, ihi, H, LDH, T, LDT, ALPHA, strideALPHA, BETA, strideBETA, Q, LDQ, Z, LDZ, WORK, strideWORK, lwork, RWORK, strideRWORK )
    Compute the eigenvalues of a complex matrix pair (H, T), where H is.

    Parameters
    ----------
    order: string
        Row-major (C-style) or column-major (Fortran-style) order.

    job: string
        `job`.

    compq: string
        `compq`.

    compz: string
        `compz`.

    N: integer
        number of columns.

    ilo: integer
        `ilo`.

    ihi: integer
        `ihi`.

    H: Float64Array
        `H`.

    LDH: integer
        leading dimension of `H`.

    T: Float64Array
        `T`.

    LDT: integer
        leading dimension of `T`.

    ALPHA: Float64Array
        `ALPHA`.

    strideALPHA: integer
        stride length for `ALPHA`.

    BETA: Float64Array
        `BETA`.

    strideBETA: integer
        stride length for `BETA`.

    Q: Float64Array
        `Q`.

    LDQ: integer
        leading dimension of `Q`.

    Z: Float64Array
        `Z`.

    LDZ: integer
        leading dimension of `Z`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    lwork: integer
        `lwork`.

    RWORK: Float64Array
        `RWORK`.

    strideRWORK: integer
        stride length for `RWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( job, compq, compz, N, ilo, ihi, H, strideH1, strideH2, offsetH, T, strideT1, strideT2, offsetT, ALPHA, strideALPHA, offsetALPHA, BETA, strideBETA, offsetBETA, Q, strideQ1, strideQ2, offsetQ, Z, strideZ1, strideZ2, offsetZ, WORK, strideWORK, offsetWORK, lwork, RWORK, strideRWORK, offsetRWORK )
    Compute the eigenvalues of a complex matrix pair (H, T), where H is
    using alternative indexing semantics.

    Parameters
    ----------
    job: string
        `job`.

    compq: string
        `compq`.

    compz: string
        `compz`.

    N: integer
        number of columns.

    ilo: integer
        `ilo`.

    ihi: integer
        `ihi`.

    H: Float64Array
        `H`.

    strideH1: integer
        stride of dimension 1 of `H`.

    strideH2: integer
        stride of dimension 2 of `H`.

    offsetH: integer
        starting index for `H`.

    T: Float64Array
        `T`.

    strideT1: integer
        stride of dimension 1 of `T`.

    strideT2: integer
        stride of dimension 2 of `T`.

    offsetT: integer
        starting index for `T`.

    ALPHA: Float64Array
        `ALPHA`.

    strideALPHA: integer
        stride length for `ALPHA`.

    offsetALPHA: integer
        starting index for `ALPHA`.

    BETA: Float64Array
        `BETA`.

    strideBETA: integer
        stride length for `BETA`.

    offsetBETA: integer
        starting index for `BETA`.

    Q: Float64Array
        `Q`.

    strideQ1: integer
        stride of dimension 1 of `Q`.

    strideQ2: integer
        stride of dimension 2 of `Q`.

    offsetQ: integer
        starting index for `Q`.

    Z: Float64Array
        `Z`.

    strideZ1: integer
        stride of dimension 1 of `Z`.

    strideZ2: integer
        stride of dimension 2 of `Z`.

    offsetZ: integer
        starting index for `Z`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    lwork: integer
        `lwork`.

    RWORK: Float64Array
        `RWORK`.

    strideRWORK: integer
        stride length for `RWORK`.

    offsetRWORK: integer
        starting index for `RWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
