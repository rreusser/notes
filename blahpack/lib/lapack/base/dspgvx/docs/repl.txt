{{alias}}( order, itype, jobz, range, uplo, N, AP, BP, vl, vu, il, iu, abstol, out, w, Z, LDZ, WORK, IWORK, IFAIL )
    @license Apache-2.0.

    Parameters
    ----------
    order: string
        Row-major (C-style) or column-major (Fortran-style) order.

    itype: integer
        `itype`.

    jobz: string
        `jobz`.

    range: string
        `range`.

    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    N: integer
        number of columns.

    AP: Float64Array
        `AP`.

    BP: Float64Array
        `BP`.

    vl: number
        `vl`.

    vu: number
        `vu`.

    il: integer
        `il`.

    iu: integer
        `iu`.

    abstol: number
        `abstol`.

    out: number
        `out`.

    w: Float64Array
        `w`.

    Z: Float64Array
        `Z`.

    LDZ: integer
        leading dimension of `Z`.

    WORK: Float64Array
        `WORK`.

    IWORK: Int32Array
        `IWORK`.

    IFAIL: Float64Array
        `IFAIL`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( itype, jobz, range, uplo, N, AP, strideAP, offsetAP, BP, strideBP, offsetBP, vl, vu, il, iu, abstol, out, w, strideW, offsetW, Z, strideZ1, strideZ2, offsetZ, WORK, strideWORK, offsetWORK, IWORK, strideIWORK, offsetIWORK, IFAIL, strideIFAIL, offsetIFAIL )
    @license Apache-2.0 using alternative indexing semantics.

    Parameters
    ----------
    itype: integer
        `itype`.

    jobz: string
        `jobz`.

    range: string
        `range`.

    uplo: string
        specifies whether the upper or lower triangular part is referenced.

    N: integer
        number of columns.

    AP: Float64Array
        `AP`.

    strideAP: integer
        stride length for `AP`.

    offsetAP: integer
        starting index for `AP`.

    BP: Float64Array
        `BP`.

    strideBP: integer
        stride length for `BP`.

    offsetBP: integer
        starting index for `BP`.

    vl: number
        `vl`.

    vu: number
        `vu`.

    il: integer
        `il`.

    iu: integer
        `iu`.

    abstol: number
        `abstol`.

    out: number
        `out`.

    w: Float64Array
        `w`.

    strideW: integer
        stride length for `W`.

    offsetW: integer
        starting index for `W`.

    Z: Float64Array
        `Z`.

    strideZ1: integer
        stride of dimension 1 of `Z`.

    strideZ2: integer
        stride of dimension 2 of `Z`.

    offsetZ: integer
        starting index for `Z`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    IWORK: Int32Array
        `IWORK`.

    strideIWORK: integer
        stride length for `IWORK`.

    offsetIWORK: integer
        starting index for `IWORK`.

    IFAIL: Float64Array
        `IFAIL`.

    strideIFAIL: integer
        stride length for `IFAIL`.

    offsetIFAIL: integer
        starting index for `IFAIL`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
