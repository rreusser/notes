{{alias}}( f, offsetF, g, offsetG, c, offsetC, s, offsetS, r, offsetR )
    Compute |re|^2 + |im|^2.

    Parameters
    ----------
    f: number
        `f`.

    offsetF: integer
        starting index for `F`.

    g: number
        `g`.

    offsetG: integer
        starting index for `G`.

    c: number
        `c`.

    offsetC: integer
        starting index for `C`.

    s: number
        `s`.

    offsetS: integer
        starting index for `S`.

    r: number
        `r`.

    offsetR: integer
        starting index for `R`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( f, offsetF, g, offsetG, c, offsetC, s, offsetS, r, offsetR )
    Compute |re|^2 + |im|^2 using alternative indexing semantics.

    Parameters
    ----------
    f: number
        `f`.

    offsetF: integer
        starting index for `F`.

    g: number
        `g`.

    offsetG: integer
        starting index for `G`.

    c: number
        `c`.

    offsetC: integer
        starting index for `C`.

    s: number
        `s`.

    offsetS: integer
        starting index for `S`.

    r: number
        `r`.

    offsetR: integer
        starting index for `R`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
