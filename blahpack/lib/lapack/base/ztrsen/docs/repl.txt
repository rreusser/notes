{{alias}}( job, compq, SELECT, strideSELECT, N, T, LDT, Q, LDQ, W, strideW, M, s, sep, WORK, strideWORK, lwork )
    Reorders the Schur factorization of a complex matrix A = Q*T*Q**H so
    that a.

    Parameters
    ----------
    job: string
        `job`.

    compq: string
        `compq`.

    SELECT: Int32Array
        `SELECT`.

    strideSELECT: integer
        stride length for `SELECT`.

    N: integer
        number of columns.

    T: Float64Array
        `T`.

    LDT: integer
        leading dimension of `T`.

    Q: Float64Array
        `Q`.

    LDQ: integer
        leading dimension of `Q`.

    W: Float64Array
        `W`.

    strideW: integer
        stride length for `W`.

    M: integer
        number of rows.

    s: number
        `s`.

    sep: number
        `sep`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    lwork: integer
        `lwork`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( job, compq, SELECT, strideSELECT, offsetSELECT, N, T, strideT1, strideT2, offsetT, Q, strideQ1, strideQ2, offsetQ, W, strideW, offsetW, M, s, sep, WORK, strideWORK, offsetWORK, lwork )
    Reorders the Schur factorization of a complex matrix A = Q*T*Q**H so
    that a using alternative indexing semantics.

    Parameters
    ----------
    job: string
        `job`.

    compq: string
        `compq`.

    SELECT: Int32Array
        `SELECT`.

    strideSELECT: integer
        stride length for `SELECT`.

    offsetSELECT: integer
        starting index for `SELECT`.

    N: integer
        number of columns.

    T: Float64Array
        `T`.

    strideT1: integer
        stride of dimension 1 of `T`.

    strideT2: integer
        stride of dimension 2 of `T`.

    offsetT: integer
        starting index for `T`.

    Q: Float64Array
        `Q`.

    strideQ1: integer
        stride of dimension 1 of `Q`.

    strideQ2: integer
        stride of dimension 2 of `Q`.

    offsetQ: integer
        starting index for `Q`.

    W: Float64Array
        `W`.

    strideW: integer
        stride length for `W`.

    offsetW: integer
        starting index for `W`.

    M: integer
        number of rows.

    s: number
        `s`.

    sep: number
        `sep`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    lwork: integer
        `lwork`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
