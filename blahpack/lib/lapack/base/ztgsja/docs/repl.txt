{{alias}}( jobu, jobv, jobq, M, p, N, K, l, A, LDA, B, LDB, tola, tolb, ALPHA, BETA, U, LDU, V, LDV, Q, LDQ, WORK, ncycle )
    Computes the generalized singular value decomposition (GSVD) of two
    complex.

    Parameters
    ----------
    jobu: string
        `jobu`.

    jobv: number
        `jobv`.

    jobq: number
        `jobq`.

    M: integer
        number of rows.

    p: number
        `p`.

    N: integer
        number of columns.

    K: integer
        inner dimension.

    l: number
        `l`.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    B: Float64Array
        `B`.

    LDB: integer
        leading dimension of `B`.

    tola: number
        `tola`.

    tolb: number
        `tolb`.

    ALPHA: Float64Array
        `ALPHA`.

    BETA: Float64Array
        `BETA`.

    U: Float64Array
        `U`.

    LDU: integer
        leading dimension of `U`.

    V: Float64Array
        `V`.

    LDV: integer
        leading dimension of `V`.

    Q: Float64Array
        `Q`.

    LDQ: integer
        leading dimension of `Q`.

    WORK: Float64Array
        `WORK`.

    ncycle: number
        `ncycle`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( jobu, jobv, jobq, M, p, N, K, l, A, strideA1, strideA2, offsetA, B, strideB1, strideB2, offsetB, tola, tolb, ALPHA, strideALPHA, offsetALPHA, BETA, strideBETA, offsetBETA, U, strideU1, strideU2, offsetU, V, strideV1, strideV2, offsetV, Q, strideQ1, strideQ2, offsetQ, WORK, strideWORK, offsetWORK, ncycle )
    Computes the generalized singular value decomposition (GSVD) of two
    complex using alternative indexing semantics.

    Parameters
    ----------
    jobu: string
        `jobu`.

    jobv: number
        `jobv`.

    jobq: number
        `jobq`.

    M: integer
        number of rows.

    p: number
        `p`.

    N: integer
        number of columns.

    K: integer
        inner dimension.

    l: number
        `l`.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    B: Float64Array
        `B`.

    strideB1: integer
        stride of dimension 1 of `B`.

    strideB2: integer
        stride of dimension 2 of `B`.

    offsetB: integer
        starting index for `B`.

    tola: number
        `tola`.

    tolb: number
        `tolb`.

    ALPHA: Float64Array
        `ALPHA`.

    strideALPHA: integer
        stride length for `ALPHA`.

    offsetALPHA: integer
        starting index for `ALPHA`.

    BETA: Float64Array
        `BETA`.

    strideBETA: integer
        stride length for `BETA`.

    offsetBETA: integer
        starting index for `BETA`.

    U: Float64Array
        `U`.

    strideU1: integer
        stride of dimension 1 of `U`.

    strideU2: integer
        stride of dimension 2 of `U`.

    offsetU: integer
        starting index for `U`.

    V: Float64Array
        `V`.

    strideV1: integer
        stride of dimension 1 of `V`.

    strideV2: integer
        stride of dimension 2 of `V`.

    offsetV: integer
        starting index for `V`.

    Q: Float64Array
        `Q`.

    strideQ1: integer
        stride of dimension 1 of `Q`.

    strideQ2: integer
        stride of dimension 2 of `Q`.

    offsetQ: integer
        starting index for `Q`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    ncycle: number
        `ncycle`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
