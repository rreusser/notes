
    zgees.ndarray( jobvs, sort, select, N, A, strideA1, strideA2, offsetA, sdim, w, strideW, offsetW, VS, strideVS1, strideVS2, offsetVS, WORK, strideWORK, offsetWORK, lwork, RWORK, strideRWORK, offsetRWORK, BWORK, strideBWORK, offsetBWORK )
        Compute eigenvalues and Schur decomposition of a complex matrix

        Parameters
        ----------
        jobvs: string
            specifies the operation type.

        sort: string
            specifies the operation type.

        select: boolean
            select.

        N: NonNegativeInteger
            number of columns.

        A: Float64Array
            input matrix.

        strideA1: integer
            stride of the first dimension of `A`.

        strideA2: integer
            stride of the second dimension of `A`.

        offsetA: NonNegativeInteger
            starting index for `A`.

        sdim: integer
            sdim.

        w: Float64Array
            input array.

        strideW: integer
            stride length for `w`.

        offsetW: NonNegativeInteger
            starting index for `w`.

        VS: Float64Array
            input matrix.

        strideVS1: integer
            stride of the first dimension of `VS`.

        strideVS2: integer
            stride of the second dimension of `VS`.

        offsetVS: NonNegativeInteger
            starting index for `VS`.

        WORK: Float64Array
            input array.

        strideWORK: integer
            stride length for `WORK`.

        offsetWORK: NonNegativeInteger
            starting index for `WORK`.

        lwork: integer
            lwork.

        RWORK: Float64Array
            input array.

        strideRWORK: integer
            stride length for `RWORK`.

        offsetRWORK: NonNegativeInteger
            starting index for `RWORK`.

        BWORK: Float64Array
            output array.

        strideBWORK: integer
            stride length for `BWORK`.

        offsetBWORK: NonNegativeInteger
            starting index for `BWORK`.

        Returns
        -------
        out: integer
            TODO: describe return value.

        Examples
        --------
        // TODO: Add REPL examples

    See Also
    --------

