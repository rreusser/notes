{{alias}}( N, A, LDA, RHS, strideRHS, IPIV, strideIPIV, JPIV, strideJPIV, scale )
    Solves a system of linear equations `A * X = scale * RHS` with a
    general.

    Parameters
    ----------
    N: integer
        number of columns.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    RHS: Float64Array
        `RHS`.

    strideRHS: integer
        stride length for `RHS`.

    IPIV: Int32Array
        `IPIV`.

    strideIPIV: integer
        stride length for `IPIV`.

    JPIV: Int32Array
        `JPIV`.

    strideJPIV: integer
        stride length for `JPIV`.

    scale: number
        `scale`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, A, strideA1, strideA2, offsetA, RHS, strideRHS, offsetRHS, IPIV, strideIPIV, offsetIPIV, JPIV, strideJPIV, offsetJPIV, scale )
    Solves a system of linear equations `A * X = scale * RHS` with a general
    using alternative indexing semantics.

    Parameters
    ----------
    N: integer
        number of columns.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    RHS: Float64Array
        `RHS`.

    strideRHS: integer
        stride length for `RHS`.

    offsetRHS: integer
        starting index for `RHS`.

    IPIV: Int32Array
        `IPIV`.

    strideIPIV: integer
        stride length for `IPIV`.

    offsetIPIV: integer
        starting index for `IPIV`.

    JPIV: Int32Array
        `JPIV`.

    strideJPIV: integer
        stride length for `JPIV`.

    offsetJPIV: integer
        starting index for `JPIV`.

    scale: number
        `scale`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
