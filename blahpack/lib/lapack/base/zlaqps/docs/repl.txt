{{alias}}( order, M, N, offset, nb, A, LDA, JPVT, strideJPVT, TAU, strideTAU, VN1, strideVN1, VN2, strideVN2, AUXV, strideAUXV, F, LDF )
    Computes a step of QR factorization with column pivoting using a.

    Parameters
    ----------
    order: string
        Row-major (C-style) or column-major (Fortran-style) order.

    M: integer
        number of rows.

    N: integer
        number of columns.

    offset: integer
        `offset`.

    nb: integer
        `nb`.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    JPVT: Float64Array
        `JPVT`.

    strideJPVT: integer
        stride length for `JPVT`.

    TAU: Float64Array
        `TAU`.

    strideTAU: integer
        stride length for `TAU`.

    VN1: Float64Array
        `VN1`.

    strideVN1: integer
        stride of dimension 1 of `VN`.

    VN2: Float64Array
        `VN2`.

    strideVN2: integer
        stride of dimension 2 of `VN`.

    AUXV: Float64Array
        `AUXV`.

    strideAUXV: integer
        stride length for `AUXV`.

    F: Float64Array
        `F`.

    LDF: integer
        leading dimension of `F`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( M, N, offset, nb, A, strideA1, strideA2, offsetA, JPVT, strideJPVT, offsetJPVT, TAU, strideTAU, offsetTAU, VN1, strideVN1, offsetVN1, VN2, strideVN2, offsetVN2, AUXV, strideAUXV, offsetAUXV, F, strideF1, strideF2, offsetF )
    Computes a step of QR factorization with column pivoting using a using
    alternative indexing semantics.

    Parameters
    ----------
    M: integer
        number of rows.

    N: integer
        number of columns.

    offset: integer
        `offset`.

    nb: integer
        `nb`.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    JPVT: Float64Array
        `JPVT`.

    strideJPVT: integer
        stride length for `JPVT`.

    offsetJPVT: integer
        starting index for `JPVT`.

    TAU: Float64Array
        `TAU`.

    strideTAU: integer
        stride length for `TAU`.

    offsetTAU: integer
        starting index for `TAU`.

    VN1: Float64Array
        `VN1`.

    strideVN1: integer
        stride of dimension 1 of `VN`.

    offsetVN1: integer
        starting index for `VN1`.

    VN2: Float64Array
        `VN2`.

    strideVN2: integer
        stride of dimension 2 of `VN`.

    offsetVN2: integer
        starting index for `VN2`.

    AUXV: Float64Array
        `AUXV`.

    strideAUXV: integer
        stride length for `AUXV`.

    offsetAUXV: integer
        starting index for `AUXV`.

    F: Float64Array
        `F`.

    strideF1: integer
        stride of dimension 1 of `F`.

    strideF2: integer
        stride of dimension 2 of `F`.

    offsetF: integer
        starting index for `F`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
