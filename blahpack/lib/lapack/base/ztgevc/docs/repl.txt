{{alias}}( order, side, howmny, SELECT, strideSELECT, offsetSELECT, N, S, LDS, P, LDP, VL, LDVL, VR, LDVR, mm, M, WORK, strideWORK, RWORK, strideRWORK )
    ABS1: |re| + |im| (cheap complex absolute value).

    Parameters
    ----------
    order: string
        Row-major (C-style) or column-major (Fortran-style) order.

    side: string
        specifies the side of the operation.

    howmny: string
        `howmny`.

    SELECT: Int32Array
        `SELECT`.

    strideSELECT: integer
        stride length for `SELECT`.

    offsetSELECT: integer
        starting index for `SELECT`.

    N: integer
        number of columns.

    S: Float64Array
        `S`.

    LDS: integer
        leading dimension of `S`.

    P: Float64Array
        `P`.

    LDP: integer
        leading dimension of `P`.

    VL: Float64Array
        `VL`.

    LDVL: integer
        leading dimension of `VL`.

    VR: Float64Array
        `VR`.

    LDVR: integer
        leading dimension of `VR`.

    mm: integer
        `mm`.

    M: integer
        number of rows.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    RWORK: Float64Array
        `RWORK`.

    strideRWORK: integer
        stride length for `RWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( side, howmny, SELECT, strideSELECT, offsetSELECT, N, S, strideS1, strideS2, offsetS, P, strideP1, strideP2, offsetP, VL, strideVL1, strideVL2, offsetVL, VR, strideVR1, strideVR2, offsetVR, mm, M, WORK, strideWORK, offsetWORK, RWORK, strideRWORK, offsetRWORK )
    ABS1: |re| + |im| (cheap complex absolute value) using alternative
    indexing semantics.

    Parameters
    ----------
    side: string
        specifies the side of the operation.

    howmny: string
        `howmny`.

    SELECT: Int32Array
        `SELECT`.

    strideSELECT: integer
        stride length for `SELECT`.

    offsetSELECT: integer
        starting index for `SELECT`.

    N: integer
        number of columns.

    S: Float64Array
        `S`.

    strideS1: integer
        stride of dimension 1 of `S`.

    strideS2: integer
        stride of dimension 2 of `S`.

    offsetS: integer
        starting index for `S`.

    P: Float64Array
        `P`.

    strideP1: integer
        stride of dimension 1 of `P`.

    strideP2: integer
        stride of dimension 2 of `P`.

    offsetP: integer
        starting index for `P`.

    VL: Float64Array
        `VL`.

    strideVL1: integer
        stride of dimension 1 of `VL`.

    strideVL2: integer
        stride of dimension 2 of `VL`.

    offsetVL: integer
        starting index for `VL`.

    VR: Float64Array
        `VR`.

    strideVR1: integer
        stride of dimension 1 of `VR`.

    strideVR2: integer
        stride of dimension 2 of `VR`.

    offsetVR: integer
        starting index for `VR`.

    mm: integer
        `mm`.

    M: integer
        number of rows.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    RWORK: Float64Array
        `RWORK`.

    strideRWORK: integer
        stride length for `RWORK`.

    offsetRWORK: integer
        starting index for `RWORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
