{{alias}}( fact, N, nrhs, d, e, DF, EF, B, LDB, X, LDX, rcond, FERR, BERR, WORK )
    @license Apache-2.0.

    Parameters
    ----------
    fact: string
        `fact`.

    N: integer
        number of columns.

    nrhs: integer
        number of right-hand sides.

    d: Float64Array
        `d`.

    e: Float64Array
        `e`.

    DF: Float64Array
        `DF`.

    EF: Float64Array
        `EF`.

    B: Float64Array
        `B`.

    LDB: integer
        leading dimension of `B`.

    X: Float64Array
        `X`.

    LDX: integer
        leading dimension of `X`.

    rcond: number
        `rcond`.

    FERR: Float64Array
        `FERR`.

    BERR: Float64Array
        `BERR`.

    WORK: Float64Array
        `WORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( fact, N, nrhs, d, strideD, offsetD, e, strideE, offsetE, DF, strideDF, offsetDF, EF, strideEF, offsetEF, B, strideB1, strideB2, offsetB, X, strideX1, strideX2, offsetX, rcond, FERR, strideFERR, offsetFERR, BERR, strideBERR, offsetBERR, WORK, strideWORK, offsetWORK )
    @license Apache-2.0 using alternative indexing semantics.

    Parameters
    ----------
    fact: string
        `fact`.

    N: integer
        number of columns.

    nrhs: integer
        number of right-hand sides.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    offsetD: integer
        starting index for `D`.

    e: Float64Array
        `e`.

    strideE: integer
        stride length for `E`.

    offsetE: integer
        starting index for `E`.

    DF: Float64Array
        `DF`.

    strideDF: integer
        stride length for `DF`.

    offsetDF: integer
        starting index for `DF`.

    EF: Float64Array
        `EF`.

    strideEF: integer
        stride length for `EF`.

    offsetEF: integer
        starting index for `EF`.

    B: Float64Array
        `B`.

    strideB1: integer
        stride of dimension 1 of `B`.

    strideB2: integer
        stride of dimension 2 of `B`.

    offsetB: integer
        starting index for `B`.

    X: Float64Array
        `X`.

    strideX1: integer
        stride of dimension 1 of `X`.

    strideX2: integer
        stride of dimension 2 of `X`.

    offsetX: integer
        starting index for `X`.

    rcond: number
        `rcond`.

    FERR: Float64Array
        `FERR`.

    strideFERR: integer
        stride length for `FERR`.

    offsetFERR: integer
        starting index for `FERR`.

    BERR: Float64Array
        `BERR`.

    strideBERR: integer
        stride length for `BERR`.

    offsetBERR: integer
        starting index for `BERR`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
