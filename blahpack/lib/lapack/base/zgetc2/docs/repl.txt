{{alias}}( N, A, LDA, IPIV, strideIPIV, JPIV, strideJPIV )
    Computes an LU factorization with complete pivoting of a general N-by-N
    complex matrix.

    Parameters
    ----------
    N: integer
        number of columns.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    IPIV: Int32Array
        `IPIV`.

    strideIPIV: integer
        stride length for `IPIV`.

    JPIV: Int32Array
        `JPIV`.

    strideJPIV: integer
        stride length for `JPIV`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, A, strideA1, strideA2, offsetA, IPIV, strideIPIV, offsetIPIV, JPIV, strideJPIV, offsetJPIV )
    Computes an LU factorization with complete pivoting of a general N-by-N
    complex matrix using alternative indexing semantics.

    Parameters
    ----------
    N: integer
        number of columns.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    IPIV: Int32Array
        `IPIV`.

    strideIPIV: integer
        stride length for `IPIV`.

    offsetIPIV: integer
        starting index for `IPIV`.

    JPIV: Int32Array
        `JPIV`.

    strideJPIV: integer
        stride length for `JPIV`.

    offsetJPIV: integer
        starting index for `JPIV`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
