
{{alias}}( job, M, N, d, strideD, SEP, strideSEP )
    Compute the reciprocal condition numbers for the eigenvectors of a real symmetric or complex Hermitian matrix

    Parameters
    ----------
        job: string
            specifies the operation type.

        M: integer
            number of rows.

        N: integer
            number of columns.

        d: Float64Array
            input array.

        strideD: integer
            stride length for `d`.

        SEP: Float64Array
            output array.

        strideSEP: integer
            stride length for `SEP`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( job, M, N, d, strideD, offsetD, SEP, strideSEP, offsetSEP )
    Compute the reciprocal condition numbers for the eigenvectors of a real symmetric or complex Hermitian matrix, using alternative indexing semantics.

    Parameters
    ----------
        job: string
            specifies the operation type.

        M: integer
            number of rows.

        N: integer
            number of columns.

        d: Float64Array
            input array.

        strideD: integer
            stride length for `d`.

        offsetD: integer
            starting index for `D`.

        SEP: Float64Array
            output array.

        strideSEP: integer
            stride length for `SEP`.

        offsetSEP: integer
            starting index for `SEP`.

    Returns
    -------
    out: integer
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
