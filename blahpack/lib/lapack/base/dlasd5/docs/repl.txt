{{alias}}( i, D, strideD, Z, strideZ, DELTA, strideDELTA, rho, dsigma, WORK, strideWORK )
    Compute the square root of the i-th eigenvalue of a positive symmetric
    rank-one modification of a 2-by-2 diagonal matrix.

    Parameters
    ----------
    i: number
        `i`.

    D: Float64Array
        `D`.

    strideD: integer
        stride length for `D`.

    Z: Float64Array
        `Z`.

    strideZ: integer
        stride length for `Z`.

    DELTA: Float64Array
        `DELTA`.

    strideDELTA: integer
        stride length for `DELTA`.

    rho: number
        `rho`.

    dsigma: number
        `dsigma`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( i, d, strideD, offsetD, z, strideZ, offsetZ, DELTA, strideDELTA, offsetDELTA, rho, dsigma, WORK, strideWORK, offsetWORK )
    Compute the square root of the i-th eigenvalue of a positive symmetric
    rank-one modification of a 2-by-2 diagonal matrix using alternative
    indexing semantics.

    Parameters
    ----------
    i: number
        `i`.

    d: Float64Array
        `d`.

    strideD: integer
        stride length for `D`.

    offsetD: integer
        starting index for `D`.

    z: Float64Array
        `z`.

    strideZ: integer
        stride length for `Z`.

    offsetZ: integer
        starting index for `Z`.

    DELTA: Float64Array
        `DELTA`.

    strideDELTA: integer
        stride length for `DELTA`.

    offsetDELTA: integer
        starting index for `DELTA`.

    rho: number
        `rho`.

    dsigma: number
        `dsigma`.

    WORK: Float64Array
        `WORK`.

    strideWORK: integer
        stride length for `WORK`.

    offsetWORK: integer
        starting index for `WORK`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
