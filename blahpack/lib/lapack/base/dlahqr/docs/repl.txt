{{alias}}( wantt, wantz, N, ilo, ihi, H, LDH, WR, strideWR, WI, strideWI, iloz, ihiz, Z, LDZ )
    Computes the eigenvalues and optionally the Schur factorization of an
    upper.

    Parameters
    ----------
    wantt: boolean
        `wantt`.

    wantz: boolean
        `wantz`.

    N: integer
        number of columns.

    ilo: integer
        `ilo`.

    ihi: integer
        `ihi`.

    H: Float64Array
        `H`.

    LDH: integer
        leading dimension of `H`.

    WR: Float64Array
        `WR`.

    strideWR: integer
        stride length for `WR`.

    WI: Float64Array
        `WI`.

    strideWI: integer
        stride length for `WI`.

    iloz: number
        `iloz`.

    ihiz: number
        `ihiz`.

    Z: Float64Array
        `Z`.

    LDZ: integer
        leading dimension of `Z`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( wantt, wantz, N, ilo, ihi, H, strideH1, strideH2, offsetH, WR, strideWR, offsetWR, WI, strideWI, offsetWI, iloz, ihiz, Z, strideZ1, strideZ2, offsetZ )
    Computes the eigenvalues and optionally the Schur factorization of an
    upper using alternative indexing semantics.

    Parameters
    ----------
    wantt: boolean
        `wantt`.

    wantz: boolean
        `wantz`.

    N: integer
        number of columns.

    ilo: integer
        `ilo`.

    ihi: integer
        `ihi`.

    H: Float64Array
        `H`.

    strideH1: integer
        stride of dimension 1 of `H`.

    strideH2: integer
        stride of dimension 2 of `H`.

    offsetH: integer
        starting index for `H`.

    WR: Float64Array
        `WR`.

    strideWR: integer
        stride length for `WR`.

    offsetWR: integer
        starting index for `WR`.

    WI: Float64Array
        `WI`.

    strideWI: integer
        stride length for `WI`.

    offsetWI: integer
        starting index for `WI`.

    iloz: number
        `iloz`.

    ihiz: number
        `ihiz`.

    Z: Float64Array
        `Z`.

    strideZ1: integer
        stride of dimension 1 of `Z`.

    strideZ2: integer
        stride of dimension 2 of `Z`.

    offsetZ: integer
        starting index for `Z`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
