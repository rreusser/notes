{{alias}}( order, N, K, nb, A, LDA, tau, strideTAU, t, strideT, ldt, y, strideY, ldy )
    Reduces NB columns of a real general n-by-(n-k+1) matrix A.

    Parameters
    ----------
    order: string
        Row-major (C-style) or column-major (Fortran-style) order.

    N: integer
        number of columns.

    K: integer
        inner dimension.

    nb: integer
        `nb`.

    A: Float64Array
        `A`.

    LDA: integer
        leading dimension of `A`.

    tau: Float64Array
        `tau`.

    strideTAU: integer
        stride length for `TAU`.

    t: Float64Array
        `t`.

    strideT: integer
        stride length for `T`.

    ldt: number
        `ldt`.

    y: Float64Array
        `y`.

    strideY: integer
        stride length for `Y`.

    ldy: number
        `ldy`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, K, nb, A, strideA1, strideA2, offsetA, tau, strideTAU, offsetTAU, T, strideT, offsetT, ldT, Y, strideY, offsetY, ldY )
    Reduces NB columns of a real general n-by-(n-k+1) matrix A using
    alternative indexing semantics.

    Parameters
    ----------
    N: integer
        number of columns.

    K: integer
        inner dimension.

    nb: integer
        `nb`.

    A: Float64Array
        `A`.

    strideA1: integer
        stride of dimension 1 of `A`.

    strideA2: integer
        stride of dimension 2 of `A`.

    offsetA: integer
        starting index for `A`.

    tau: Float64Array
        `tau`.

    strideTAU: integer
        stride length for `TAU`.

    offsetTAU: integer
        starting index for `TAU`.

    T: Float64Array
        `T`.

    strideT: integer
        stride length for `T`.

    offsetT: integer
        starting index for `T`.

    ldT: number
        `ldT`.

    Y: Float64Array
        `Y`.

    strideY: integer
        stride length for `Y`.

    offsetY: integer
        starting index for `Y`.

    ldY: number
        `ldY`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
